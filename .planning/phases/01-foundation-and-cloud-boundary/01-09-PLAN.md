---
phase: 1
plan: "01-09"
title: "Manual release constraints and secret-safe documentation"
type: standard
wave: 3
depends_on:
  - "01-04"
autonomous: true
requirements:
  - AUTH-01
files_modified:
  - apps/cloud-functions/cloudfunctions.json
  - scripts/release-dev.sh
  - scripts/release-prod.sh
  - docs/release/cloudbase-and-miniapp.md
  - .gitignore
must_haves:
  truths:
    - prod 发布不是默认路径，必须手动确认。
    - 发布文档明确说明 secrets 位置和禁忌项。
    - .gitignore 覆盖发布相关敏感文件。
  artifacts:
    - apps/cloud-functions/cloudfunctions.json
    - scripts/release-dev.sh
    - scripts/release-prod.sh
    - docs/release/cloudbase-and-miniapp.md
    - .gitignore
  key_links:
    - cloudfunctions.json -> named CloudBase functions bootstrapUser/bindPhone/assertMerchantAccess
    - release-dev/prod scripts -> CloudBase env templates and function deployment
    - release docs -> secret-safe local setup
---

<objective>
把 Phase 1 的发布约束单独拆成小计划，避免和 UI 壳层混在一起造成 scope 膨胀。
</objective>

<threat_model>
  <assets>
    - 发布脚本
    - 发布文档
    - 敏感文件忽略规则
  </assets>
  <entry_points>
    - apps/cloud-functions/cloudfunctions.json
    - scripts/release-dev.sh
    - scripts/release-prod.sh
    - release docs
    - .gitignore
  </entry_points>
  <threat severity="high">发布脚本默认指向 prod，或无需人工确认即可执行。</threat>
  <threat severity="medium">发布相关 secrets/私钥被提交进仓库。</threat>
  <mitigations>
    - `release-prod.sh` 必须显式 `prod` 参数和人工确认。
    - `.gitignore` 覆盖 env、私钥、开发者工具私有配置和云缓存。
  </mitigations>
</threat_model>

<tasks>
  <task id="T1" type="auto">
    <name>Create dev/prod release scripts with manual prod gate</name>
    <files>
      - apps/cloud-functions/cloudfunctions.json
      - scripts/release-dev.sh
      - scripts/release-prod.sh
    </files>
    <read_first>
      - apps/cloud-functions/cloudbaserc.json
      - apps/cloud-functions/.env.dev.example
      - apps/cloud-functions/.env.prod.example
    </read_first>
    <action>
      创建 `apps/cloud-functions/cloudfunctions.json`、`release-dev.sh` 和 `release-prod.sh`。`cloudfunctions.json` 必须显式声明 `bootstrapUser`、`bindPhone`、`assertMerchantAccess` 三个函数的源目录/发布名映射；`release-dev.sh` 只允许 dev 环境，并包含打包/发布这三个函数以及导入/应用 CloudBase collections、indexes、security 配置的明确步骤；`release-prod.sh` 必须要求显式 `prod` 参数和人工确认文本，缺失确认时返回非 0，并明确提示不得默认发布到 prod。
    </action>
    <verify>
      <automated>test -f apps/cloud-functions/cloudfunctions.json && test -f scripts/release-dev.sh && test -f scripts/release-prod.sh && rg -n 'bootstrapUser|bindPhone|assertMerchantAccess|dev|prod|confirm|手动|collections|indexes|security' apps/cloud-functions/cloudfunctions.json scripts/release-dev.sh scripts/release-prod.sh scripts/release-dev.sh scripts/release-prod.sh</automated>
    </verify>
    <acceptance_criteria>
      - cloudfunctions.json 已把三个函数映射成可发布的 CloudBase 函数。
      - dev 脚本包含函数发布和配置导入步骤。
      - dev/prod 脚本行为被显式区分。
    </acceptance_criteria>
    <done>正式发布不再是默认脚本路径。</done>
  </task>

  <task id="T2" type="auto">
    <name>Document release flow and harden ignore rules</name>
    <files>
      - docs/release/cloudbase-and-miniapp.md
      - .gitignore
    </files>
    <read_first>
      - /Users/zhangyi/zhangyi/homework/xiaipet/.planning/PROJECT.md
      - .gitignore
    </read_first>
    <action>
      编写 `docs/release/cloudbase-and-miniapp.md`，记录 CloudBase 环境切换、小程序上传、`collections/indexes/security` 配置如何通过脚本或控制台导入到环境、敏感配置装配位置和禁忌项；同时更新 `.gitignore`，确保 `.env*`、私钥、开发者工具私有配置和云缓存不入仓。
    </action>
    <verify>
      <automated>test -f docs/release/cloudbase-and-miniapp.md && test -f .gitignore && rg -n '敏感配置|CloudBase|prod|\.env|\.key' docs/release/cloudbase-and-miniapp.md .gitignore</automated>
    </verify>
    <acceptance_criteria>
      - 发布文档说明 secrets 位置和禁忌项。
      - .gitignore 覆盖发布相关敏感文件。
    </acceptance_criteria>
    <done>发布约束已通过文档和忽略规则固化。</done>
  </task>
</tasks>

<verification>
  <step>`bash scripts/release-prod.sh` 在无确认参数时返回非 0 并提示人工确认</step>
  <step>`rg -n '敏感配置|\.env|\.key|collections|indexes|security' docs/release/cloudbase-and-miniapp.md .gitignore` 验证 secret-safe 约束</step>
</verification>

<success_criteria>
  - prod 发布约束和 secret-safe 文档已独立落地，不再挤占 UI 计划范围。
</success_criteria>
