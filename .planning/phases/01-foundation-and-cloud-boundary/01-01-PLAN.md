---
phase: 1
plan: "01-01"
title: "Workspace baseline and Wave 0 verification scaffold"
type: standard
wave: 0
depends_on: []
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.base.json
  - .gitignore
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/vitest.config.ts
  - packages/shared/src/schema/user-record.test.ts
  - packages/shared/src/rules/user-bootstrap.test.ts
must_haves:
  truths:
    - 根工作区能识别 apps/packages 结构并提供统一脚本入口。
    - Wave 0 已经为 shared 规则与 schema 准备好最小测试脚手架。
    - 敏感本地环境文件和上传密钥不会进入仓库。
  artifacts:
    - package.json
    - pnpm-workspace.yaml
    - tsconfig.base.json
    - .gitignore
    - packages/shared/vitest.config.ts
    - packages/shared/src/schema/user-record.test.ts
    - packages/shared/src/rules/user-bootstrap.test.ts
  key_links:
    - root package.json scripts -> pnpm workspace packages
    - shared vitest config -> shared test files
    - .gitignore -> env/key/cache exclusion
---

<objective>
先建立可执行的 workspace 与 Wave 0 验证脚手架，避免后续计划只能写代码、没有快速自动反馈。
</objective>

<threat_model>
  <assets>
    - workspace 根配置
    - shared 测试脚手架
    - 本地敏感文件忽略规则
  </assets>
  <entry_points>
    - root scripts
    - vitest config
    - .gitignore
  </entry_points>
  <threat severity="high">没有 Wave 0 测试脚手架，后续 schema/rule 任务无法满足 Nyquist 自动验证要求。</threat>
  <threat severity="medium">本地 env、上传密钥或 CloudBase 缓存被提交进仓库。</threat>
  <mitigations>
    - 在执行开始前创建 shared Vitest 配置和最小测试文件。
    - 根脚本统一暴露 lint/typecheck/test。
    - .gitignore 显式忽略 `.env*`、`*.key`、CloudBase 本地缓存与小程序构建产物。
  </mitigations>
</threat_model>

<tasks>
  <task id="T1" type="auto">
    <name>Initialize root workspace and ignore rules</name>
    <files>
      - package.json
      - pnpm-workspace.yaml
      - tsconfig.base.json
      - .gitignore
    </files>
    <read_first>
      - /Users/zhangyi/zhangyi/homework/xiaipet/.planning/ROADMAP.md
      - /Users/zhangyi/zhangyi/homework/xiaipet/.planning/phases/01-foundation-and-cloud-boundary/01-CONTEXT.md
      - /Users/zhangyi/zhangyi/homework/xiaipet/.planning/phases/01-foundation-and-cloud-boundary/01-RESEARCH.md
    </read_first>
    <action>
      按 D-01、D-02 创建根 `package.json`、`pnpm-workspace.yaml`、`tsconfig.base.json` 和 `.gitignore`。根脚本至少提供 `lint`、`typecheck`、`test`、`dev:customer`、`dev:merchant`、`dev:functions`、`ci:check`。workspace 必须只包含 `apps/customer-miniapp`、`apps/merchant-miniapp`、`apps/cloud-functions`、`packages/shared`。`.gitignore` 要忽略 `.env*`、`*.key`、CloudBase 本地缓存、小程序 dist、上传私钥和开发者工具私有配置。
    </action>
    <verify>
      <automated>test -f package.json && test -f pnpm-workspace.yaml && test -f tsconfig.base.json && test -f .gitignore && rg -n "apps/customer-miniapp|apps/merchant-miniapp|apps/cloud-functions|packages/shared" package.json pnpm-workspace.yaml tsconfig.base.json && rg -n "\.env|\.key|cloudbase|dist" .gitignore</automated>
    </verify>
    <acceptance_criteria>
      - 根工作区只使用 apps/packages 结构。
      - 根脚本存在统一 lint/typecheck/test 入口。
      - 忽略规则覆盖环境文件、密钥和缓存。
    </acceptance_criteria>
    <done>后续所有子工程和测试脚手架都能依赖同一套根配置运行。</done>
  </task>

  <task id="T2" type="auto">
    <name>Create shared Wave 0 Vitest scaffold</name>
    <files>
      - packages/shared/package.json
      - packages/shared/tsconfig.json
      - packages/shared/vitest.config.ts
      - packages/shared/src/schema/user-record.test.ts
      - packages/shared/src/rules/user-bootstrap.test.ts
    </files>
    <read_first>
      - package.json
      - tsconfig.base.json
      - /Users/zhangyi/zhangyi/homework/xiaipet/.planning/phases/01-foundation-and-cloud-boundary/01-VALIDATION.md
    </read_first>
    <action>
      在 `packages/shared` 里建立 Wave 0 所需测试基础：`package.json` 和 `tsconfig.json` 对接根 workspace；`vitest.config.ts` 提供 shared 规则与 schema 的快速测试入口；新增 `user-record.test.ts` 与 `user-bootstrap.test.ts` 两个最小测试文件，分别为 D-12 最小用户主记录字段和 D-13 懒创建规则提供后续可扩展断言模板。
    </action>
    <verify>
      <automated>test -f packages/shared/vitest.config.ts && test -f packages/shared/src/schema/user-record.test.ts && test -f packages/shared/src/rules/user-bootstrap.test.ts && rg -n "vitest|describe|it\(" packages/shared/vitest.config.ts packages/shared/src/schema/user-record.test.ts packages/shared/src/rules/user-bootstrap.test.ts</automated>
    </verify>
    <acceptance_criteria>
      - shared 包已经具备单独执行测试的配置。
      - 至少两个测试文件存在并可作为 Wave 0 依赖。
      - Validation strategy 中要求的 Wave 0 测试脚手架已经有落点。
    </acceptance_criteria>
    <done>Phase 1 的所有后续任务都可以引用 shared 的自动化快速验证命令。</done>
  </task>
</tasks>

<verification>
  <step>`pnpm install --ignore-scripts` 可以解析根 workspace 与 shared package</step>
  <step>`pnpm --filter @xiaipet/shared test -- --runInBand` 或等价 vitest 命令可以识别两份 Wave 0 测试文件</step>
  <step>`rg -n "\.env|\.key|cloudbase" .gitignore` 确认敏感文件已被忽略</step>
</verification>

<success_criteria>
  - Phase 1 已具备 Wave 0 验证脚手架。
  - apps/packages 根结构与敏感文件忽略规则已经建立。
</success_criteria>
