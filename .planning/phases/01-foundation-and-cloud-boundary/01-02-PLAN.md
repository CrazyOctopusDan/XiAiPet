---
phase: 1
plan: "01-02"
title: "Mini program and cloud-function shell configuration"
type: standard
wave: 1
depends_on:
  - "01-01"
autonomous: true
requirements:
  - AUTH-01
files_modified:
  - apps/customer-miniapp/package.json
  - apps/customer-miniapp/project.config.json
  - apps/customer-miniapp/tsconfig.json
  - apps/merchant-miniapp/package.json
  - apps/merchant-miniapp/project.config.json
  - apps/merchant-miniapp/tsconfig.json
  - apps/cloud-functions/package.json
  - apps/cloud-functions/tsconfig.json
must_haves:
  truths:
    - 双端与云函数都拥有独立且可继续实现的工程壳层。
    - 双端 miniapp 作为 PNPM workspace member 有明确 manifest。
    - cloud-functions 工作区已能接入根 workspace。
  artifacts:
    - apps/customer-miniapp/package.json
    - apps/customer-miniapp/project.config.json
    - apps/customer-miniapp/tsconfig.json
    - apps/merchant-miniapp/package.json
    - apps/merchant-miniapp/project.config.json
    - apps/merchant-miniapp/tsconfig.json
    - apps/cloud-functions/package.json
    - apps/cloud-functions/tsconfig.json
  key_links:
    - miniapp package.json -> root pnpm workspace recursive scripts
    - miniapp project config -> child tsconfig.json and later app/page plans
    - cloud-functions package -> root workspace scripts
---

<objective>
把客户端、商户端和云函数工程骨架显式落到文件层，让执行阶段不再靠隐含配置拼接。
</objective>

<threat_model>
  <assets>
    - 双端小程序基础配置
    - 双端 workspace manifests
    - 云函数 workspace 接入点
  </assets>
  <entry_points>
    - package.json
    - project.config.json
    - child tsconfig.json
    - cloud-functions package.json
  </entry_points>
  <threat severity="medium">双端目录没有 package.json，根 `pnpm -r` 脚本无法真正覆盖这些 app。</threat>
  <threat severity="medium">缺失 child tsconfig，导致 shared 路径和 TypeScript 基线无法在双端复用。</threat>
  <mitigations>
    - 客户端、商户端都显式拥有 package.json / project.config.json / tsconfig.json。
    - app.json 和页面路由交由后续页面计划显式创建与注册，避免隐式页面依赖。
  </mitigations>
</threat_model>

<tasks>
  <task id="T1" type="auto">
    <name>Scaffold customer and merchant mini program workspace members</name>
    <files>
      - apps/customer-miniapp/package.json
      - apps/customer-miniapp/project.config.json
      - apps/customer-miniapp/tsconfig.json
      - apps/merchant-miniapp/package.json
      - apps/merchant-miniapp/project.config.json
      - apps/merchant-miniapp/tsconfig.json
    </files>
    <read_first>
      - package.json
      - pnpm-workspace.yaml
      - /Users/zhangyi/zhangyi/homework/xiaipet/.planning/phases/01-foundation-and-cloud-boundary/01-UI-SPEC.md
      - /Users/zhangyi/zhangyi/homework/xiaipet/.planning/phases/01-foundation-and-cloud-boundary/01-CONTEXT.md
    </read_first>
    <action>
      按 D-02 和 UI-SPEC 创建双端小程序基础配置。两个 app 都必须显式拥有 `package.json`、`project.config.json`、`tsconfig.json`；`package.json` 至少提供 `typecheck` 与本地 dev 脚本，保证根 `pnpm -r typecheck` 能覆盖到双端。此计划不负责 `app.json` 与具体页面注册，后续页面计划会显式创建并验证这些入口文件。
    </action>
    <verify>
      <automated>test -f apps/customer-miniapp/package.json && test -f apps/customer-miniapp/project.config.json && test -f apps/customer-miniapp/tsconfig.json && test -f apps/merchant-miniapp/package.json && test -f apps/merchant-miniapp/project.config.json && test -f apps/merchant-miniapp/tsconfig.json && rg -n 'typecheck|dev|miniprogramRoot|extends' apps/customer-miniapp/package.json apps/customer-miniapp/project.config.json apps/customer-miniapp/tsconfig.json apps/merchant-miniapp/package.json apps/merchant-miniapp/project.config.json apps/merchant-miniapp/tsconfig.json</automated>
    </verify>
    <acceptance_criteria>
      - 双端都拥有 workspace manifest、project config、child tsconfig。
      - 双端 manifest 足以被 `pnpm -r` 识别。
    </acceptance_criteria>
    <done>双端基础壳层已纳入 workspace 递归命令。</done>
  </task>

  <task id="T2" type="auto">
    <name>Wire cloud-functions workspace into the monorepo</name>
    <files>
      - apps/cloud-functions/package.json
      - apps/cloud-functions/tsconfig.json
    </files>
    <read_first>
      - package.json
      - pnpm-workspace.yaml
      - tsconfig.base.json
    </read_first>
    <action>
      创建 `apps/cloud-functions/package.json` 与 `apps/cloud-functions/tsconfig.json`，把云函数工作区接入根 workspace，保留后续 `bootstrapUser`、`bindPhone`、`assertMerchantAccess` 和共享 helper 的编译入口。这里只做工程接入，不提前写环境或业务逻辑文件。
    </action>
    <verify>
      <automated>test -f apps/cloud-functions/package.json && test -f apps/cloud-functions/tsconfig.json && rg -n 'name|scripts|extends' apps/cloud-functions/package.json apps/cloud-functions/tsconfig.json</automated>
    </verify>
    <acceptance_criteria>
      - 云函数工作区有独立 package/tsconfig。
      - 根 workspace 能发现 cloud-functions 子包。
    </acceptance_criteria>
    <done>CloudBase 后续实现已经有明确的代码落点。</done>
  </task>
</tasks>

<verification>
  <step>`find apps -maxdepth 2 \( -name package.json -o -name project.config.json -o -name tsconfig.json \) | sort` 覆盖双端和云函数</step>
  <step>`pnpm -r typecheck` 能识别双端与云函数工作区</step>
</verification>

<success_criteria>
  - 双端和云函数的基础工程壳层都已显式落盘。
  - 双端已具备 workspace manifests，不再是假定的 pnpm member。
</success_criteria>
