---
phase: 1
plan: "01-07"
title: "Customer phone-binding shell and secure contact persistence"
type: standard
wave: 5
depends_on:
  - "01-05"
  - "01-06"
autonomous: true
requirements:
  - AUTH-02
files_modified:
  - apps/customer-miniapp/app.json
  - apps/customer-miniapp/pages/contact-bind/index.ts
  - apps/customer-miniapp/pages/contact-bind/index.wxml
  - apps/customer-miniapp/pages/contact-bind/index.wxss
  - apps/customer-miniapp/src/services/phone.ts
must_haves:
  truths:
    - AUTH-02 同时具备微信手机号能力和手动补录入口。
    - AUTH-02 存在明确的服务端持久化路径 `phone.ts -> bindPhone`。
    - customer app.json 在页面存在之后才注册 `pages/contact-bind/index`。
  artifacts:
    - apps/customer-miniapp/app.json
    - apps/customer-miniapp/pages/contact-bind/index.ts
    - apps/customer-miniapp/pages/contact-bind/index.wxml
    - apps/customer-miniapp/pages/contact-bind/index.wxss
    - apps/customer-miniapp/src/services/phone.ts
  key_links:
    - customer app.json -> `pages/contact-bind/index`
    - contact-bind page -> phone service -> bindPhone cloud function
---

<objective>
把 AUTH-02 补成完整链路：页面路由、双路径交互、以及服务端持久化全部明确。
</objective>

<threat_model>
  <assets>
    - 手机号绑定入口
    - 联系方式持久化服务路径
    - customer contact-bind route
  </assets>
  <entry_points>
    - app.json
    - contact-bind page
    - phone service
    - bindPhone cloud function
  </entry_points>
  <threat severity="high">手机号能力只停留在前端，不写回受控后端路径。</threat>
  <threat severity="medium">页面路由注册早于页面本身存在，导致运行时不可达。</threat>
  <mitigations>
    - phone service 显式调用 bindPhone 持久化。
    - contact-bind 路由在本计划和页面文件同时落地。
  </mitigations>
</threat_model>

<tasks>
  <task id="T1" type="auto">
    <name>Create contact-bind route and page shell with dual entry paths</name>
    <files>
      - apps/customer-miniapp/app.json
      - apps/customer-miniapp/pages/contact-bind/index.ts
      - apps/customer-miniapp/pages/contact-bind/index.wxml
      - apps/customer-miniapp/pages/contact-bind/index.wxss
    </files>
    <read_first>
      - apps/customer-miniapp/app.json
      - /Users/zhangyi/zhangyi/homework/xiaipet/.planning/phases/01-foundation-and-cloud-boundary/01-UI-SPEC.md
      - apps/customer-miniapp/src/theme/tokens.ts
    </read_first>
    <action>
      在页面文件存在的同一计划里更新 `app.json`，显式注册 `pages/contact-bind/index`；同时实现 `contact-bind` 页面壳层，并在 `pages/contact-bind/index.ts` 中导入 `src/services/phone.ts` 的 `requestWechatPhone` 与 `submitManualPhone` 两个函数，让微信手机号授权和手动补录两条路径都通过同一 service 层进入后端。页面使用已存在的 customer tokens 与 UI-SPEC 中的错误态/空态文案约束。
    </action>
    <verify>
      <automated>test -f apps/customer-miniapp/app.json && test -f apps/customer-miniapp/pages/contact-bind/index.ts && rg -n 'pages/contact-bind/index|getPhoneNumber|手动补录|requestWechatPhone|submitManualPhone|customerSurface|customerAccent' apps/customer-miniapp/app.json apps/customer-miniapp/pages/contact-bind/index.ts apps/customer-miniapp/pages/contact-bind/index.wxml apps/customer-miniapp/pages/contact-bind/index.wxss apps/customer-miniapp/src/theme/tokens.ts</automated>
    </verify>
    <acceptance_criteria>
      - app.json 在页面文件存在之后注册 contact-bind route。
      - 页面同时具备授权和手动补录两条入口。
      - 页面复用已存在的 customer tokens。
    </acceptance_criteria>
    <done>AUTH-02 的 UI 路由和页面入口已经完整存在。</done>
  </task>

  <task id="T2" type="auto">
    <name>Wire phone service to secure bindPhone persistence</name>
    <files>
      - apps/customer-miniapp/src/services/phone.ts
    </files>
    <read_first>
      - apps/cloud-functions/src/bindPhone/index.ts
      - packages/shared/src/schema/phone-binding.ts
      - apps/customer-miniapp/src/theme/tokens.ts
    </read_first>
    <action>
      实现 `src/services/phone.ts`，显式导出 `requestWechatPhone` 与 `submitManualPhone` 两个函数，把微信手机号能力返回值和手动补录数据统一规范化后提交到 `bindPhone` 云函数，确保只更新 shared schema 已定义的 `phoneBindingState`、`contactPhoneMasked`、`contactPhoneCountryCode` 三个字段；不要在此任务重复创建 tokens 文件，而是显式依赖 01-06 已创建的 customer tokens。
    </action>
    <verify>
      <automated>test -f apps/customer-miniapp/src/services/phone.ts && rg -n 'requestWechatPhone|submitManualPhone|bindPhone|phoneBindingState|contactPhoneMasked|contactPhoneCountryCode' apps/customer-miniapp/src/services/phone.ts apps/cloud-functions/src/bindPhone/index.ts</automated>
    </verify>
    <acceptance_criteria>
      - phone service 明确调用 bindPhone 云函数。
      - phoneBindingState 的更新不再只是“预留输入”。
    </acceptance_criteria>
    <done>AUTH-02 已具备从 UI 到服务端的完整安全持久化路径。</done>
  </task>
</tasks>

<verification>
  <step>`rg -n 'pages/contact-bind/index' apps/customer-miniapp/app.json` 验证 route 注册时机</step>
  <step>`rg -n 'bindPhone|phoneBindingState|contactPhoneMasked|contactPhoneCountryCode' apps/customer-miniapp/src/services/phone.ts apps/cloud-functions/src/bindPhone/index.ts` 验证安全持久化路径</step>
</verification>

<success_criteria>
  - AUTH-02 不再只是前端占位，而是完整的前后端链路。
</success_criteria>
