---
phase: 1
plan: "01-06"
title: "Customer launch shell and bootstrap routing"
type: standard
wave: 4
depends_on:
  - "01-02"
  - "01-03"
  - "01-05"
autonomous: true
requirements:
  - AUTH-01
files_modified:
  - apps/customer-miniapp/app.ts
  - apps/customer-miniapp/app.json
  - apps/customer-miniapp/pages/launch/index.ts
  - apps/customer-miniapp/pages/launch/index.wxml
  - apps/customer-miniapp/pages/launch/index.wxss
  - apps/customer-miniapp/src/services/auth.ts
  - apps/customer-miniapp/src/theme/tokens.ts
must_haves:
  truths:
    - 客户端首屏可以建立或恢复微信身份。
    - customer app.json 只注册已在本计划落地的 `pages/launch/index` 路由。
    - customer theme tokens 在 launch/contact-bind 之前就已存在。
    - 客户端壳层遵守已批准的品牌化 UI-SPEC。
  artifacts:
    - apps/customer-miniapp/app.ts
    - apps/customer-miniapp/app.json
    - apps/customer-miniapp/pages/launch/index.ts
    - apps/customer-miniapp/pages/launch/index.wxml
    - apps/customer-miniapp/pages/launch/index.wxss
    - apps/customer-miniapp/src/services/auth.ts
    - apps/customer-miniapp/src/theme/tokens.ts
  key_links:
    - customer app.json -> `pages/launch/index`
    - app.ts -> launch page -> auth service -> bootstrapUser
    - customer theme tokens -> launch page and later contact-bind page
---

<objective>
把 AUTH-01 的客户端启动链路做成可执行页面壳层，并为后续 contact-bind 页面准备已存在的 customer token 资产。
</objective>

<threat_model>
  <assets>
    - 客户端登录态
    - customer app page routes
    - 品牌化 UI token 引用入口
  </assets>
  <entry_points>
    - app.ts
    - app.json
    - launch page
    - auth service
    - customer theme tokens
  </entry_points>
  <threat severity="high">页面文件存在但没有注册到 app.json，导致入口不可达。</threat>
  <threat severity="high">客户端绕过云函数直接写身份相关集合。</threat>
  <mitigations>
    - 在本计划显式修改 app.json 并只注册当前已存在的 launch route。
    - auth service 只通过 bootstrapUser 完成身份恢复/创建。
    - customer theme tokens 在任何页面 read_first/verify 引用前先落地。
  </mitigations>
</threat_model>

<tasks>
  <task id="T1" type="auto">
    <name>Register launch route and initialize customer bootstrap</name>
    <files>
      - apps/customer-miniapp/app.ts
      - apps/customer-miniapp/app.json
      - apps/customer-miniapp/src/services/auth.ts
    </files>
    <read_first>
      - apps/customer-miniapp/package.json
      - apps/cloud-functions/src/bootstrapUser/index.ts
      - /Users/zhangyi/zhangyi/homework/xiaipet/.planning/phases/01-foundation-and-cloud-boundary/01-UI-SPEC.md
    </read_first>
    <action>
      更新 `app.ts`、`app.json`、`src/services/auth.ts`。`app.ts` 初始化 CloudBase 与全局 auth 状态；`auth.ts` 通过 `wx.login` 调用 `bootstrapUser`；`app.json` 仅显式注册 `pages/launch/index` 这一条当前已存在的客户页面路由，不提前注册 contact-bind。
    </action>
    <verify>
      <automated>test -f apps/customer-miniapp/app.ts && test -f apps/customer-miniapp/app.json && test -f apps/customer-miniapp/src/services/auth.ts && rg -n 'pages/launch/index|bootstrapUser|wx.login' apps/customer-miniapp/app.ts apps/customer-miniapp/app.json apps/customer-miniapp/src/services/auth.ts && ! rg -n 'pages/contact-bind/index' apps/customer-miniapp/app.json</automated>
    </verify>
    <acceptance_criteria>
      - customer app.json 已明确注册 launch route。
      - auth service 通过 bootstrapUser 完成 AUTH-01。
      - 不会在本计划提前注册尚未存在的 contact-bind 页面。
    </acceptance_criteria>
    <done>客户 app 的启动路由和身份引导链路已经建立。</done>
  </task>

  <task id="T2" type="auto">
    <name>Build brand-like launch page shell and customer tokens</name>
    <files>
      - apps/customer-miniapp/pages/launch/index.ts
      - apps/customer-miniapp/pages/launch/index.wxml
      - apps/customer-miniapp/pages/launch/index.wxss
      - apps/customer-miniapp/src/theme/tokens.ts
    </files>
    <read_first>
      - /Users/zhangyi/zhangyi/homework/xiaipet/.planning/phases/01-foundation-and-cloud-boundary/01-UI-SPEC.md
    </read_first>
    <action>
      按 UI-SPEC 的 customer brand-like 方向实现 launch 页面壳层，并创建 `src/theme/tokens.ts`。`pages/launch/index.ts` 必须导入 `src/services/auth.ts` 中的 `startCustomerBootstrap`，在主 CTA 点击和首屏状态同步时调用它；页面展示 welcome headline、`微信授权登录` 主 CTA、身份状态卡和环境徽标。验证必须使用 token 名称 `customerSurface`、`customerSecondary`、`customerAccent`，或 UI-SPEC 中批准的颜色值 `#F6E396` / `#E7D7C3` / `#D96C4E`。
    </action>
    <verify>
      <automated>test -f apps/customer-miniapp/pages/launch/index.ts && test -f apps/customer-miniapp/src/theme/tokens.ts && rg -n '微信授权登录|startCustomerBootstrap|customerSurface|customerSecondary|customerAccent|F6E396|E7D7C3|D96C4E' apps/customer-miniapp/pages/launch/index.ts apps/customer-miniapp/pages/launch/index.wxml apps/customer-miniapp/pages/launch/index.wxss apps/customer-miniapp/src/theme/tokens.ts</automated>
    </verify>
    <acceptance_criteria>
      - launch 页具备品牌化首屏和明确登录动作。
      - pages/launch/index.ts 已明确调用 `startCustomerBootstrap`，而不是只停留在展示型 CTA。
      - 验证命令引用的是 UI-SPEC 批准的 token 或颜色值。
    </acceptance_criteria>
    <done>客户首屏已符合 UI-SPEC，且 customer tokens 已前置落地。</done>
  </task>
</tasks>

<verification>
  <step>`rg -n 'pages/launch/index' apps/customer-miniapp/app.json` 验证 launch 路由注册</step>
  <step>`rg -n '微信授权登录|customerSurface|customerAccent' apps/customer-miniapp/pages/launch apps/customer-miniapp/src/theme/tokens.ts` 验证 AUTH-01 入口和 UI token</step>
</verification>

<success_criteria>
  - AUTH-01 客户端入口、launch 路由和 customer tokens 都已显式建立。
</success_criteria>
