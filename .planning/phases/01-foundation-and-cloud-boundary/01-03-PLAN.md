---
phase: 1
plan: "01-03"
title: "Shared domain contracts and pure business rules"
type: standard
wave: 1
depends_on:
  - "01-01"
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
files_modified:
  - packages/shared/src/index.ts
  - packages/shared/src/types/user.ts
  - packages/shared/src/constants/env.ts
  - packages/shared/src/schema/runtime-config.ts
  - packages/shared/src/schema/user-record.ts
  - packages/shared/src/schema/merchant-user.ts
  - packages/shared/src/schema/phone-binding.ts
  - packages/shared/src/rules/user-bootstrap.ts
must_haves:
  truths:
    - shared 只承载类型、常量、schema 和纯规则函数。
    - 最小用户主记录、商户白名单、手机号绑定输入都有统一 schema。
    - shared 内不存在任何数据访问实现。
  artifacts:
    - packages/shared/src/index.ts
    - packages/shared/src/types/user.ts
    - packages/shared/src/constants/env.ts
    - packages/shared/src/schema/runtime-config.ts
    - packages/shared/src/schema/user-record.ts
    - packages/shared/src/schema/merchant-user.ts
    - packages/shared/src/schema/phone-binding.ts
    - packages/shared/src/rules/user-bootstrap.ts
  key_links:
    - shared index -> type/schema/rule exports
    - user-record schema -> bootstrapUser function inputs/outputs
    - merchant-user schema -> assertMerchantAccess inputs/outputs
---

<objective>
把 D-03、D-04、D-11、D-12、D-13 需要的 shared 纯契约建立好，并用文件结构保证 data access 不会混进 shared。
</objective>

<threat_model>
  <assets>
    - shared 导出边界
    - 用户/商户/手机号 schema
    - 纯业务规则函数
  </assets>
  <entry_points>
    - packages/shared/src/index.ts
    - packages/shared/src/schema/*
    - packages/shared/src/rules/*
  </entry_points>
  <threat severity="high">把 CloudBase、wx.cloud 或数据库查询逻辑塞入 shared，破坏 D-04。</threat>
  <threat severity="medium">最小用户主记录字段定义不完整，后续 AUTH-01/AUTH-02 无统一承载。</threat>
  <mitigations>
    - shared 只出现 type/schema/rule/constant 四类文件。
    - verify 里用 grep 证明 shared 不含数据访问关键字。
  </mitigations>
</threat_model>

<tasks>
  <task id="T1" type="auto">
    <name>Create shared export surface and core types</name>
    <files>
      - packages/shared/src/index.ts
      - packages/shared/src/types/user.ts
      - packages/shared/src/constants/env.ts
      - packages/shared/src/schema/runtime-config.ts
    </files>
    <read_first>
      - packages/shared/package.json
      - packages/shared/tsconfig.json
      - /Users/zhangyi/zhangyi/homework/xiaipet/.planning/phases/01-foundation-and-cloud-boundary/01-CONTEXT.md
    </read_first>
    <action>
      按 D-03 在 shared 中建立统一导出面：`index.ts` 汇总 type/schema/rule/constant 导出；`types/user.ts` 定义身份相关公共类型；`constants/env.ts` 定义 dev/prod 等环境常量；`schema/runtime-config.ts` 定义运行时配置 schema。动作必须显式说明 shared 不提供任何 CloudBase 数据访问封装。
    </action>
    <verify>
      <automated>test -f packages/shared/src/index.ts && test -f packages/shared/src/types/user.ts && test -f packages/shared/src/constants/env.ts && test -f packages/shared/src/schema/runtime-config.ts && rg -n "export|dev|prod" packages/shared/src/index.ts packages/shared/src/types/user.ts packages/shared/src/constants/env.ts packages/shared/src/schema/runtime-config.ts</automated>
    </verify>
    <acceptance_criteria>
      - shared 已建立统一导出入口。
      - 环境常量和运行时配置 schema 已具备明确文件落点。
      - shared 的职责被限制在纯契约层。
    </acceptance_criteria>
    <done>双端和云函数后续都能从 shared 读取一致的类型和环境常量。</done>
  </task>

  <task id="T2" type="auto">
    <name>Define user, merchant, and phone-binding schemas with pure rules</name>
    <files>
      - packages/shared/src/schema/user-record.ts
      - packages/shared/src/schema/merchant-user.ts
      - packages/shared/src/schema/phone-binding.ts
      - packages/shared/src/rules/user-bootstrap.ts
    </files>
    <read_first>
      - /Users/zhangyi/zhangyi/homework/xiaipet/.planning/REQUIREMENTS.md
      - /Users/zhangyi/zhangyi/homework/xiaipet/.planning/phases/01-foundation-and-cloud-boundary/01-RESEARCH.md
      - packages/shared/src/schema/user-record.test.ts
      - packages/shared/src/rules/user-bootstrap.test.ts
    </read_first>
    <action>
      按 D-11、D-12、D-13 定义最小用户主记录 schema（至少 `openid`、`status`、`createdAt`，并补充 `updatedAt`、`lastLoginAt`、`phoneBindingState`、`contactPhoneMasked`、`contactPhoneCountryCode`）；其中 `contactPhoneMasked` 存放规范化后的展示值，`contactPhoneCountryCode` 存放区号/国家码，作为 AUTH-02 唯一允许写回用户主记录的联系方式字段。同步定义商户白名单 schema、手机号绑定输入 schema；再在 `rules/user-bootstrap.ts` 写入纯规则函数，明确首次登录只创建核心用户主记录，不初始化 `profile`、`pets`、`addresses`、`balance`、会员相关集合。按 D-04，shared 内禁止出现 `wx.cloud`、`database(`、`@cloudbase`、`tcb-admin`。
    </action>
    <verify>
      <automated>test -f packages/shared/src/schema/user-record.ts && test -f packages/shared/src/schema/merchant-user.ts && test -f packages/shared/src/schema/phone-binding.ts && test -f packages/shared/src/rules/user-bootstrap.ts && rg -n "openid|status|createdAt|phoneBindingState|contactPhoneMasked|contactPhoneCountryCode|merchant|whitelist" packages/shared/src/schema/user-record.ts packages/shared/src/schema/merchant-user.ts packages/shared/src/schema/phone-binding.ts packages/shared/src/rules/user-bootstrap.ts && ! rg -n "wx\.cloud|database\(|@cloudbase|tcb-admin" packages/shared/src</automated>
    </verify>
    <acceptance_criteria>
      - 最小用户主记录字段覆盖 AUTH-01/AUTH-02 所需核心状态，并明确 AUTH-02 仅允许写回 `contactPhoneMasked` 与 `contactPhoneCountryCode`。
      - 白名单与手机号绑定输入有独立 schema。
      - 懒创建原则被写进纯规则函数而不是口头约定。
    </acceptance_criteria>
    <done>shared 的纯契约已经可以被云函数和前端共同依赖。</done>
  </task>
</tasks>

<verification>
  <step>`pnpm --filter @xiaipet/shared test` 覆盖 user-record 和 user-bootstrap 基础规则</step>
  <step>`! rg -n "wx\.cloud|database\(|@cloudbase|tcb-admin" packages/shared/src` 保证无数据访问泄漏</step>
</verification>

<success_criteria>
  - shared 纯契约和纯规则已经可用。
  - D-03/D-04/D-11/D-12/D-13 在 shared 层有明确实现落点。
</success_criteria>
