---
phase: 7
plan: 07-02
name: Config errors and logging foundation
type: execute
wave: 1
depends_on:
  - 07-01
files_modified:
  - apps/api/.env.example
  - apps/api/src/config/env.ts
  - apps/api/src/config/env.test.ts
  - apps/api/src/lib/errors.ts
  - apps/api/src/lib/errors.test.ts
  - apps/api/src/lib/logger.ts
  - apps/api/src/app.ts
autonomous: true
requirements:
  - BE-01
  - BE-03
---

<objective>
Add typed environment parsing, stable API error envelopes and Fastify logger options without requiring real cloud credentials.
</objective>

<threat_model>
T7-02: Secrets are committed or exposed through error/log output.
Severity: high.
Mitigation: `.env.example` uses placeholders only; config parser redacts sensitive keys in errors; error envelope returns safe codes and messages.
</threat_model>

<tasks>
  <task id="07-02-01" type="standard">
    <read_first>
      <file>apps/cloud-functions/src/shared/env.ts</file>
      <file>.planning/phases/07-node-api-foundation-and-ecs-deployment-runway/07-CONTEXT.md</file>
    </read_first>
    <action>
      Create `apps/api/src/config/env.ts` with `loadApiConfig(raw = process.env)`. Supported fields for Phase 7: `NODE_ENV`, `API_HOST`, `API_PORT`, `LOG_LEVEL`, `API_PUBLIC_BASE_URL`. Defaults: `NODE_ENV=development`, `API_HOST=0.0.0.0`, `API_PORT=3000`, `LOG_LEVEL=info`. Validate `API_PORT` is an integer 1-65535 and `LOG_LEVEL` is one of `fatal,error,warn,info,debug,trace,silent`.
    </action>
    <verify>
      <automated>pnpm --filter @xiaipet/api test -- --run</automated>
    </verify>
    <acceptance_criteria>
      Invalid ports and invalid log levels throw deterministic config errors; valid minimal config returns a typed object.
    </acceptance_criteria>
  </task>

  <task id="07-02-02" type="standard">
    <read_first>
      <file>apps/api/src/config/env.ts</file>
    </read_first>
    <action>
      Create `apps/api/src/config/env.test.ts` covering defaults, valid override, invalid port, invalid log level, and placeholder public base URL. Tests must not depend on real environment variables.
    </action>
    <verify>
      <automated>pnpm --filter @xiaipet/api test -- --run src/config/env.test.ts</automated>
    </verify>
    <acceptance_criteria>
      Config tests pass without RDS, OSS, WeChat or payment credentials.
    </acceptance_criteria>
  </task>

  <task id="07-02-03" type="standard">
    <read_first>
      <file>.planning/phases/07-node-api-foundation-and-ecs-deployment-runway/07-RESEARCH.md</file>
    </read_first>
    <action>
      Create `apps/api/src/lib/errors.ts` with `ApiError`, `toErrorResponse(error)`, and response shape `{ ok: false, code: string, message: string }`. Unknown errors should become `INTERNAL_ERROR` with a generic message. Register a Fastify error handler in `apps/api/src/app.ts`.
    </action>
    <verify>
      <automated>pnpm --filter @xiaipet/api test -- --run src/lib/errors.test.ts</automated>
    </verify>
    <acceptance_criteria>
      Unknown errors do not expose stack traces or raw messages in the HTTP response.
    </acceptance_criteria>
  </task>

  <task id="07-02-04" type="standard">
    <read_first>
      <file>apps/api/src/config/env.ts</file>
      <file>Fastify logging docs from 07-RESEARCH.md</file>
    </read_first>
    <action>
      Create `apps/api/src/lib/logger.ts` with `createLoggerOptions(config)` returning Fastify logger config. Use JSON logging for production and readable development logging if the dependency is available; otherwise keep a simple level-based logger config. Ensure sensitive headers or env-like keys are redacted when configured.
    </action>
    <verify>
      <automated>pnpm --filter @xiaipet/api typecheck</automated>
    </verify>
    <acceptance_criteria>
      `buildApp()` uses logger options derived from config instead of hard-coded `logger: true`.
    </acceptance_criteria>
  </task>

  <task id="07-02-05" type="standard">
    <read_first>
      <file>.gitignore</file>
      <file>apps/api/src/config/env.ts</file>
    </read_first>
    <action>
      Add `apps/api/.env.example` documenting safe placeholders for Phase 7 config. Confirm `.env`, `.env.local`, `.env.production`, and secret-like files remain ignored or add scoped ignore rules if missing.
    </action>
    <verify>
      <automated>git check-ignore apps/api/.env apps/api/.env.production</automated>
    </verify>
    <acceptance_criteria>
      Example file contains no real secrets and actual env files are ignored.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run:
- `pnpm --filter @xiaipet/api typecheck`
- `pnpm --filter @xiaipet/api test`
- `git check-ignore apps/api/.env apps/api/.env.production`
</verification>

<success_criteria>
- API config validates safely and has tests.
- Error responses are stable and do not leak internals.
- Logger setup is derived from validated config.
- Secret-bearing env files are ignored.
</success_criteria>
