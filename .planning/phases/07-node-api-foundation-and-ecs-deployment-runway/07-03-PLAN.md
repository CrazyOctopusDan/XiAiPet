---
phase: 7
plan: 07-03
name: Docker Compose deployment skeleton
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - apps/api/Dockerfile
  - apps/api/.dockerignore
  - docker-compose.yml
  - apps/api/package.json
autonomous: true
requirements:
  - BE-02
  - BE-03
---

<objective>
Add a production-oriented Dockerfile and root Docker Compose skeleton for the unified API service.
</objective>

<threat_model>
T7-03: Docker image or Compose file bakes secrets into the image or repository.
Severity: high.
Mitigation: Dockerfile never copies `.env*`; Compose uses `env_file` and variable names only; production secrets stay on ECS.
</threat_model>

<tasks>
  <task id="07-03-01" type="standard">
    <read_first>
      <file>package.json</file>
      <file>pnpm-workspace.yaml</file>
      <file>apps/api/package.json</file>
      <file>Docker Node.js guide from 07-RESEARCH.md</file>
    </read_first>
    <action>
      Create `apps/api/Dockerfile` using a Node LTS base image. Build from repo root context so `packages/shared` and workspace manifests are available. Install pnpm through Corepack when possible. Run `pnpm install --frozen-lockfile`, build `@xiaipet/api`, prune or run production start command, and expose port 3000. Use non-root `node` user if compatible with build output.
    </action>
    <verify>
      <automated>test -f apps/api/Dockerfile</automated>
    </verify>
    <acceptance_criteria>
      Dockerfile references the unified `@xiaipet/api` package and does not reference customer/merchant split services.
    </acceptance_criteria>
  </task>

  <task id="07-03-02" type="standard">
    <read_first>
      <file>.gitignore</file>
      <file>apps/api/.env.example</file>
    </read_first>
    <action>
      Create `apps/api/.dockerignore` excluding `node_modules`, `dist`, `.env*`, logs, coverage, local caches and generated build artifacts. Keep source, workspace manifests and `packages/shared` available from the repo-root build context.
    </action>
    <verify>
      <automated>rg "\\.env\\*" apps/api/.dockerignore</automated>
    </verify>
    <acceptance_criteria>
      Docker build context excludes env files and local dependency folders.
    </acceptance_criteria>
  </task>

  <task id="07-03-03" type="standard">
    <read_first>
      <file>.planning/phases/07-node-api-foundation-and-ecs-deployment-runway/07-CONTEXT.md</file>
      <file>apps/api/.env.example</file>
    </read_first>
    <action>
      Create root `docker-compose.yml` with service `api`, build context `.`, dockerfile `apps/api/Dockerfile`, `env_file: ./apps/api/.env.production`, restart policy, port mapping `3000:3000`, and a healthcheck that calls `/health`. Do not add MySQL, OSS, Nginx, customer backend or merchant backend services in Phase 7.
    </action>
    <verify>
      <automated>docker compose config</automated>
    </verify>
    <acceptance_criteria>
      Compose defines exactly one application service named `api` for Phase 7 and no committed literal secrets.
    </acceptance_criteria>
  </task>

  <task id="07-03-04" type="standard">
    <read_first>
      <file>apps/api/package.json</file>
      <file>apps/api/src/server.ts</file>
    </read_first>
    <action>
      Ensure `apps/api/package.json` includes production-compatible `build` and `start` scripts. `build` should compile TypeScript into `dist`; `start` should run the compiled server entrypoint.
    </action>
    <verify>
      <automated>pnpm --filter @xiaipet/api build</automated>
    </verify>
    <acceptance_criteria>
      API can be built without watch mode or development-only command requirements.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run:
- `pnpm --filter @xiaipet/api build`
- `docker compose config` if Docker is available
- `rg "customer-api|merchant-api|AKIA|SECRET|PASSWORD=" docker-compose.yml apps/api/Dockerfile apps/api/.dockerignore`
</verification>

<success_criteria>
- API has a Dockerfile.
- Root Compose starts one unified API service.
- Env files are not baked into the image.
- Build/start scripts support compiled production startup.
</success_criteria>
