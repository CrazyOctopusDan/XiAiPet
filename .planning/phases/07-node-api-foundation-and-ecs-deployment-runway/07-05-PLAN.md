---
phase: 7
plan: 07-05
name: Workspace verification and Phase 7 closeout
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
  - 07-03
  - 07-04
files_modified:
  - package.json
  - .planning/STATE.md
autonomous: true
requirements:
  - BE-01
  - BE-02
  - BE-03
  - BE-04
  - BE-05
---

<objective>
Wire API checks into root workspace scripts, run Phase 7 verification and update GSD state for execution readiness.
</objective>

<threat_model>
T7-05: Verification passes while the API package is excluded from workspace checks.
Severity: medium.
Mitigation: root `pnpm -r` checks include `@xiaipet/api`; explicit filter commands are documented and run.
</threat_model>

<tasks>
  <task id="07-05-01" type="standard">
    <read_first>
      <file>package.json</file>
      <file>pnpm-workspace.yaml</file>
      <file>apps/api/package.json</file>
    </read_first>
    <action>
      Add root script `dev:api` as `pnpm --filter @xiaipet/api dev`. Confirm existing root `lint`, `typecheck`, `test`, and `ci:check` naturally include `apps/api` through `pnpm -r`.
    </action>
    <verify>
      <automated>pnpm --filter @xiaipet/api typecheck && pnpm --filter @xiaipet/api test</automated>
    </verify>
    <acceptance_criteria>
      `pnpm --filter @xiaipet/api` resolves and API checks are runnable from repo root.
    </acceptance_criteria>
  </task>

  <task id="07-05-02" type="standard">
    <read_first>
      <file>.planning/phases/07-node-api-foundation-and-ecs-deployment-runway/07-VALIDATION.md</file>
      <file>.planning/phases/07-node-api-foundation-and-ecs-deployment-runway/07-01-PLAN.md</file>
      <file>.planning/phases/07-node-api-foundation-and-ecs-deployment-runway/07-02-PLAN.md</file>
      <file>.planning/phases/07-node-api-foundation-and-ecs-deployment-runway/07-03-PLAN.md</file>
      <file>.planning/phases/07-node-api-foundation-and-ecs-deployment-runway/07-04-PLAN.md</file>
    </read_first>
    <action>
      Run and record Phase 7 verification commands during execution: `pnpm --filter @xiaipet/api typecheck`, `pnpm --filter @xiaipet/api test`, `pnpm --filter @xiaipet/api build`, `docker compose config` if Docker is installed, and docs grep checks from Plan 07-04.
    </action>
    <verify>
      <automated>pnpm --filter @xiaipet/api typecheck && pnpm --filter @xiaipet/api test && pnpm --filter @xiaipet/api build</automated>
    </verify>
    <acceptance_criteria>
      Phase 7 cannot be marked complete if API typecheck/test/build fail.
    </acceptance_criteria>
  </task>

  <task id="07-05-03" type="standard">
    <read_first>
      <file>.planning/STATE.md</file>
      <file>.planning/ROADMAP.md</file>
    </read_first>
    <action>
      After implementation succeeds, update `.planning/STATE.md` current position to Phase 7 execution/verification status and record any manual Docker or ECS limitations encountered. Do not modify unrelated business-code state.
    </action>
    <verify>
      <automated>rg "Phase 7|Node API Foundation" .planning/STATE.md</automated>
    </verify>
    <acceptance_criteria>
      GSD state reflects the actual Phase 7 execution state.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run:
- `pnpm --filter @xiaipet/api typecheck`
- `pnpm --filter @xiaipet/api test`
- `pnpm --filter @xiaipet/api build`
- `docker compose config` if Docker is available
- `git status --short` to confirm only intended files changed during execution
</verification>

<success_criteria>
- Root scripts expose API development.
- API package participates in workspace checks.
- Phase 7 verification commands are explicit and runnable.
- GSD state is updated only after implementation verification.
</success_criteria>
