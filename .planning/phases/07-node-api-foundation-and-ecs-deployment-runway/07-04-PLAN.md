---
phase: 7
plan: 07-04
name: ECS deployment runbook
type: execute
wave: 2
depends_on:
  - 07-03
files_modified:
  - docs/release/alibaba-ecs-api.md
autonomous: true
requirements:
  - BE-02
  - BE-03
  - BE-04
---

<objective>
Write the ECS Docker Compose deployment runbook for a frontend developer operator.
</objective>

<threat_model>
T7-04: Deployment docs teach unsafe practices such as committing secrets, pasting credentials into command history, or exposing diagnostics publicly.
Severity: high.
Mitigation: docs use placeholders, server-only env file paths, least-privilege reminders and safe health checks.
</threat_model>

<tasks>
  <task id="07-04-01" type="standard">
    <read_first>
      <file>docs/release/cloudbase-and-miniapp.md</file>
      <file>docker-compose.yml</file>
      <file>apps/api/.env.example</file>
      <file>.planning/phases/07-node-api-foundation-and-ecs-deployment-runway/07-RESEARCH.md</file>
    </read_first>
    <action>
      Create `docs/release/alibaba-ecs-api.md` with sections: purpose, architecture, prerequisites, ECS directory layout, first-time Docker/Compose install reference, production env file creation, deploy/start command, health check command, logs command, restart command, rollback command, and troubleshooting.
    </action>
    <verify>
      <automated>test -f docs/release/alibaba-ecs-api.md</automated>
    </verify>
    <acceptance_criteria>
      A frontend developer can identify the next command to run for start, logs, restart and rollback.
    </acceptance_criteria>
  </task>

  <task id="07-04-02" type="standard">
    <read_first>
      <file>docs/release/alibaba-ecs-api.md</file>
      <file>.planning/PROJECT.md</file>
    </read_first>
    <action>
      Add explicit safety notes: no real secrets in git, `.env.production` lives only on ECS, RDS/OSS credentials are placeholders in docs, `api.xiaipet.vip` HTTPS and WeChat legal domain are Phase 12, and IP-only access is development-only during备案.
    </action>
    <verify>
      <automated>rg "备案|api\\.xiaipet\\.vip|\\.env\\.production|no real secrets|不要" docs/release/alibaba-ecs-api.md</automated>
    </verify>
    <acceptance_criteria>
      Runbook reflects current domain filing reality and does not claim production release is ready in Phase 7.
    </acceptance_criteria>
  </task>

  <task id="07-04-03" type="standard">
    <read_first>
      <file>docker-compose.yml</file>
      <file>apps/api/src/routes/health.ts</file>
    </read_first>
    <action>
      Add a minimal smoke test checklist to the runbook: `docker compose ps`, `docker compose logs api --tail=100`, and `curl http://127.0.0.1:3000/health`. Include expected response shape without secrets.
    </action>
    <verify>
      <automated>rg "docker compose ps|docker compose logs api|/health" docs/release/alibaba-ecs-api.md</automated>
    </verify>
    <acceptance_criteria>
      Runbook contains concrete verification commands with expected outcomes.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run:
- `rg "docker compose|rollback|logs|api.xiaipet.vip|备案|health" docs/release/alibaba-ecs-api.md`
- Manual read-through for clarity and absence of real secrets.
</verification>

<success_criteria>
- ECS runbook exists.
- Runbook covers install, env, start, logs, restart, rollback and smoke verification.
- Runbook makes production-domain dependency clear.
</success_criteria>
