---
phase: 7
plan: 07-01
name: Scaffold unified API project
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/vitest.config.ts
  - apps/api/src/app.ts
  - apps/api/src/server.ts
  - apps/api/src/routes/health.ts
  - apps/api/src/routes/health.test.ts
autonomous: true
requirements:
  - BE-01
  - BE-05
---

<objective>
Create one unified `apps/api` Fastify TypeScript project with a testable app factory, server entrypoint and `/health` route.
</objective>

<threat_model>
T7-01: Health route or startup output leaks environment values.
Severity: medium.
Mitigation: health response returns only status, service name and uptime; no config dump, env dump or request headers.
</threat_model>

<tasks>
  <task id="07-01-01" type="standard">
    <read_first>
      <file>pnpm-workspace.yaml</file>
      <file>package.json</file>
      <file>tsconfig.base.json</file>
      <file>apps/cloud-functions/package.json</file>
      <file>apps/cloud-functions/tsconfig.json</file>
      <file>apps/cloud-functions/vitest.config.ts</file>
      <file>.planning/phases/07-node-api-foundation-and-ecs-deployment-runway/07-CONTEXT.md</file>
      <file>.planning/phases/07-node-api-foundation-and-ecs-deployment-runway/07-PATTERNS.md</file>
    </read_first>
    <action>
      Add `apps/api` to `pnpm-workspace.yaml`. Create `apps/api/package.json` with package name `@xiaipet/api`, scripts `dev`, `build`, `start`, `lint`, `typecheck`, and `test`, dependencies `fastify`, and dev dependencies needed for runtime TypeScript execution if not already available at root. Create `apps/api/tsconfig.json` extending `../../tsconfig.base.json`, with Node types, `rootDir: "src"`, `outDir: "dist"`, and tests excluded from build output.
    </action>
    <verify>
      <automated>pnpm --filter @xiaipet/api typecheck</automated>
    </verify>
    <acceptance_criteria>
      `pnpm --filter @xiaipet/api typecheck` resolves the new workspace package and fails only if source files are not yet added; after this plan is complete it must pass.
    </acceptance_criteria>
  </task>

  <task id="07-01-02" type="standard">
    <read_first>
      <file>apps/cloud-functions/vitest.config.ts</file>
      <file>tsconfig.base.json</file>
    </read_first>
    <action>
      Create `apps/api/vitest.config.ts` using the existing backend Vitest pattern. Set `environment: "node"` and alias `@xiaipet/shared` to `../../packages/shared/src/index.ts`.
    </action>
    <verify>
      <automated>pnpm --filter @xiaipet/api test</automated>
    </verify>
    <acceptance_criteria>
      Vitest can discover API tests under `apps/api/src/**/*.test.ts`.
    </acceptance_criteria>
  </task>

  <task id="07-01-03" type="standard">
    <read_first>
      <file>.planning/phases/07-node-api-foundation-and-ecs-deployment-runway/07-RESEARCH.md</file>
    </read_first>
    <action>
      Create `apps/api/src/routes/health.ts` as a Fastify plugin that registers `GET /health` and returns `{ ok: true, service: "xiaipet-api", uptimeSeconds: number }`. Create `apps/api/src/app.ts` exporting `buildApp()` that creates one Fastify instance and registers the health route. Create `apps/api/src/server.ts` that imports `buildApp`, listens on env-derived host/port defaults, logs startup errors and exits non-zero on startup failure.
    </action>
    <verify>
      <automated>pnpm --filter @xiaipet/api typecheck</automated>
    </verify>
    <acceptance_criteria>
      `apps/api/src/app.ts` does not call `listen`; `apps/api/src/server.ts` is the only startup entrypoint.
    </acceptance_criteria>
  </task>

  <task id="07-01-04" type="standard">
    <read_first>
      <file>apps/api/src/app.ts</file>
      <file>apps/api/src/routes/health.ts</file>
    </read_first>
    <action>
      Create `apps/api/src/routes/health.test.ts` using Fastify `app.inject()` to assert `GET /health` returns HTTP 200, `ok: true`, `service: "xiaipet-api"`, and does not include environment variable names or secret-like fields.
    </action>
    <verify>
      <automated>pnpm --filter @xiaipet/api test</automated>
    </verify>
    <acceptance_criteria>
      Health route test passes and proves the app can be constructed without binding a port.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run:
- `pnpm --filter @xiaipet/api typecheck`
- `pnpm --filter @xiaipet/api test`
- `pnpm --filter @xiaipet/api build`
</verification>

<success_criteria>
- `apps/api` exists as a single unified package.
- No `apps/customer-api` or `apps/merchant-api` package exists.
- `/health` works via app injection.
- API source compiles and tests pass.
</success_criteria>
