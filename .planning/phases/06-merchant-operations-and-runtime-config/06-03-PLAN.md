---
phase: 06-merchant-operations-and-runtime-config
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/user-admin.ts
  - packages/shared/src/schema/user-admin.ts
  - packages/shared/src/schema/user-admin.test.ts
  - packages/shared/src/types/runtime-config.ts
  - packages/shared/src/schema/runtime-config.ts
  - packages/shared/src/schema/runtime-config.test.ts
autonomous: true
requirements:
  - MUSR-01
  - MUSR-02
  - OPS-01
user_setup: []
must_haves:
  truths:
    - Merchant user search and balance adjustment payloads are explicit, audited, and cannot express a negative resulting balance per D-19 through D-26.
    - Runtime config sections are modeled as fixed-key independent documents with section-level metadata per D-27 through D-33.
    - Membership tiers require threshold, name, and description fields together per D-29.
    - Delivery-fee rules are modeled as visible explainer rows with the locked tier copy per D-32 and D-33, not hidden algorithm-only config.
  artifacts:
    - path: packages/shared/src/types/user-admin.ts
      provides: Merchant user search and balance-adjustment payloads
    - path: packages/shared/src/schema/user-admin.ts
      provides: Balance-adjustment validation including reason enum and negative-balance rejection
    - path: packages/shared/src/types/runtime-config.ts
      provides: Fixed-key runtime config section contracts including tier threshold/name/description fields and delivery-fee explainer rows
    - path: packages/shared/src/schema/runtime-config.ts
      provides: Validation for store info, delivery-fee explainer rows, membership tiers, banner, and custom notice payloads
  key_links:
    - from: packages/shared/src/schema/user-admin.ts
      to: future balance-adjustment handler
      via: transaction-safe admin payload validation
      pattern: "reasonType|targetBalance|delta"
    - from: packages/shared/src/schema/runtime-config.ts
      to: future runtime-config handlers and customer readers
      via: shared fixed-key section payloads
      pattern: "store-profile|delivery-rules|membership-tiers|banner|custom-notice"
---

<objective>
Lock the shared contracts for merchant user operations and runtime config sections before backend work starts.

Purpose: Keep balance governance auditable and make OPS-01 section saves explicit instead of letting config shape drift across handlers and pages.
Output: Shared user-admin and runtime-config types/schemas with focused tests.
</objective>

<execution_context>
@/Users/zhangyi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangyi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-merchant-operations-and-runtime-config/06-CONTEXT.md
@.planning/phases/06-merchant-operations-and-runtime-config/06-RESEARCH.md
@apps/cloud-functions/src/shared/payment-store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define merchant user search and balance-adjustment schemas with full audit fields</name>
  <files>packages/shared/src/types/user-admin.ts, packages/shared/src/schema/user-admin.ts, packages/shared/src/schema/user-admin.test.ts</files>
  <behavior>
    - Test 1: search payload supports explicit phone-or-name lookup and returns only the D-19 lightweight projection.
    - Test 2: balance adjustments require action type, reason enum, note, operator metadata, before/after balances, and reject resulting balances below zero.
    - Test 3: direct set-balance remains a first-class action equal to add/deduct per D-20 and D-21.
  </behavior>
  <action>Create shared merchant-user contracts that encode the search response shape and the audited balance-adjustment payload. Keep the reason type enum fixed to D-23, require the confirmation-friendly metadata from D-22 and D-24, and reject negative target outcomes at schema level before backend logic runs.</action>
  <verify>
    <automated>pnpm --filter @xiaipet/shared test -- user-admin</automated>
  </verify>
  <acceptance_criteria>
    - Search payloads stay limited to the D-19 lightweight projection.
    - Balance adjustment contracts require full D-22 audit fields and reject negative outcomes per D-26.
    - Direct set-balance remains a first-class action alongside add and deduct per D-20 and D-21.
  </acceptance_criteria>
  <done>Merchant user contracts now express the full audit payload, explicit search projection, and non-negative balance constraints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define fixed-key runtime config section contracts for section-scoped saves</name>
  <files>packages/shared/src/types/runtime-config.ts, packages/shared/src/schema/runtime-config.ts, packages/shared/src/schema/runtime-config.test.ts</files>
  <behavior>
    - Test 1: runtime config uses fixed section IDs for `store-profile`, `delivery-rules`, `membership-tiers`, `banner`, and `custom-notice`.
    - Test 2: delivery rules preserve the exact D-33 tier copy as visible explainer rows per D-32 instead of arbitrary freeform structure.
    - Test 3: membership tiers require threshold, name, and description fields together per D-29.
    - Test 4: banner persists `fileID` metadata and custom notice preserves hidden-but-retained text when disabled per D-30 and D-31.
  </behavior>
  <action>Model OPS-01 as independently saved fixed-key section documents, not one monolithic blob. Encode the exact delivery-fee explainer rows from D-32 and D-33, section-level `updatedBy`/`updatedAt` metadata, banner `fileID` storage, the store-profile constraint that excludes editable shop name per D-28, and the D-29 membership-tier shape of threshold + name + description so later handlers and pages cannot collapse those fields away.</action>
  <verify>
    <automated>pnpm --filter @xiaipet/shared test -- runtime-config</automated>
  </verify>
  <acceptance_criteria>
    - Runtime config uses fixed-key independently saveable sections per D-27.
    - Membership tiers require D-29 threshold, name, and description fields in the shared contract.
    - Delivery fees are represented as D-32 explainer rows with the exact D-33 locked copy, not opaque formula-only config.
  </acceptance_criteria>
  <done>Runtime config section contracts are explicit, independently saveable, and ready for backend handlers plus customer-facing readers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merchant admin payload -> shared schemas | Untrusted admin search, balance, and config payloads must be validated before reaching backend writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | T/R | `packages/shared/src/schema/user-admin.ts` | mitigate | Require full adjustment audit payloads and reject negative results so later financial handlers cannot silently mutate balances. |
| T-06-03-02 | T/I | `packages/shared/src/schema/runtime-config.ts` | mitigate | Fix section IDs, required delivery tiers, and banner `fileID` storage to prevent config drift or expiring asset URLs. |
</threat_model>

<verification>
- `pnpm --filter @xiaipet/shared test -- user-admin`
- `pnpm --filter @xiaipet/shared test -- runtime-config`
</verification>

<success_criteria>
- Merchant user and runtime config payloads are fully specified before backend work.
- OPS-01 section-level save semantics, D-29 membership tier fields, and D-32 delivery-fee explainer rows are encoded in shared validation, not deferred to page logic.
</success_criteria>

<output>
After completion, create `.planning/phases/06-merchant-operations-and-runtime-config/06-03-SUMMARY.md`
</output>
