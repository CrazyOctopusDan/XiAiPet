---
phase: 06-merchant-operations-and-runtime-config
plan: 4
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - apps/cloud-functions/src/shared/order-store.ts
  - apps/cloud-functions/src/queryMerchantOrders/index.ts
  - apps/cloud-functions/src/queryMerchantOrders/index.test.ts
  - apps/cloud-functions/src/getMerchantOrderDetail/index.ts
  - apps/cloud-functions/src/getMerchantOrderDetail/index.test.ts
  - apps/cloud-functions/src/updateMerchantOrderStatus/index.ts
  - apps/cloud-functions/src/updateMerchantOrderStatus/index.test.ts
  - apps/cloud-functions/config/indexes/orders.index.json
autonomous: true
requirements:
  - MORD-01
  - MORD-02
user_setup: []
must_haves:
  truths:
    - Merchant order list and detail are backend-owned, grouped by fulfillment progress, and include manual settlement audit detail per D-04 through D-10.
    - Paid orders use D-05 and D-06 fulfillment-mode-specific state flows in backend reads and mutations instead of one shared minimal paid-status model.
    - Merchant status changes enforce valid transitions and terminal-state locks.
  artifacts:
    - path: apps/cloud-functions/src/queryMerchantOrders/index.ts
      provides: Merchant-only grouped order query
    - path: apps/cloud-functions/src/getMerchantOrderDetail/index.ts
      provides: Merchant-only detailed order read with audit timeline
    - path: apps/cloud-functions/src/updateMerchantOrderStatus/index.ts
      provides: Audited fulfillment mutation handler
    - path: apps/cloud-functions/config/indexes/orders.index.json
      provides: Merchant order query index support
  key_links:
    - from: apps/cloud-functions/src/queryMerchantOrders/index.ts
      to: apps/cloud-functions/src/shared/order-store.ts
      via: merchant list query path
      pattern: "listMerchant|group"
    - from: apps/cloud-functions/src/updateMerchantOrderStatus/index.ts
      to: packages/shared/src/rules/order-fulfillment.ts
      via: shared transition validation
      pattern: "transition|manualSettlement"
---

<objective>
Implement the backend order governance surface for merchant order list/detail reads and status mutations.

Purpose: Deliver MORD-01 and MORD-02 through Cloud Functions instead of direct merchant collection access.
Output: Merchant order query/detail/status handlers, store extensions, and order indexes.
</objective>

<execution_context>
@/Users/zhangyi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangyi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-merchant-operations-and-runtime-config/06-CONTEXT.md
@.planning/phases/06-merchant-operations-and-runtime-config/06-RESEARCH.md
@packages/shared/src/types/order.ts
@packages/shared/src/rules/order-fulfillment.ts
@apps/cloud-functions/src/shared/order-store.ts
@apps/cloud-functions/src/assertMerchantAccess/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement merchant order list and detail handlers with grouped fulfillment reads</name>
  <files>apps/cloud-functions/src/shared/order-store.ts, apps/cloud-functions/src/queryMerchantOrders/index.ts, apps/cloud-functions/src/queryMerchantOrders/index.test.ts, apps/cloud-functions/src/getMerchantOrderDetail/index.ts, apps/cloud-functions/src/getMerchantOrderDetail/index.test.ts</files>
  <behavior>
    - Test 1: merchant order list requires merchant auth and groups records by D-06 fulfillment progress, not payment result.
    - Test 2: merchant detail response exposes timeline and manual settlement audit without leaking unrelated fields.
  </behavior>
  <action>Extend `order-store` with merchant-safe list/detail accessors. Implement `queryMerchantOrders` and `getMerchantOrderDetail` behind `assertMerchantAccess`, using the shared fulfillment helper for grouping and labels so list/detail payloads stay aligned with customer copy per D-10 and keep D-05 paid orders on fulfillment-mode-specific flows rather than a shared minimal status model.</action>
  <verify>
    <automated>pnpm --filter @xiaipet/cloud-functions test -- queryMerchantOrders getMerchantOrderDetail</automated>
  </verify>
  <acceptance_criteria>
    - Merchant list and detail handlers return D-05 and D-06 mode-specific fulfillment states for paid orders.
    - Merchant detail responses expose manual settlement audit data without leaking unrelated fields.
    - Grouping remains fulfillment-progress-first per D-04, not payment-result-first.
  </acceptance_criteria>
  <done>Merchant order queries are authenticated, grouped by fulfillment progress, and expose the audit detail needed by the detail page.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement audited status mutation and merchant order indexing</name>
  <files>apps/cloud-functions/src/updateMerchantOrderStatus/index.ts, apps/cloud-functions/src/updateMerchantOrderStatus/index.test.ts, apps/cloud-functions/config/indexes/orders.index.json</files>
  <behavior>
    - Test 1: valid next-state changes pass and append audit timeline entries.
    - Test 2: manual settlement fallback requires adjustment method, reason note, and operator metadata.
    - Test 3: terminal states reject further mutation.
  </behavior>
  <action>Implement `updateMerchantOrderStatus` so non-terminal transitions remain flexible but terminal states stay locked per D-09. Manual unpaid fallback must write the dedicated merchant override record from Plan 06-01. Enforce D-05 and D-06 explicitly in backend mutations: paid `delivery`, `pickup`, and `express` orders each follow their own fulfillment chain instead of a shared minimal status path, and use the order index file for efficient merchant grouping queries.</action>
  <verify>
    <automated>pnpm --filter @xiaipet/cloud-functions test -- updateMerchantOrderStatus</automated>
  </verify>
  <acceptance_criteria>
    - Valid next-state mutations follow the D-05 and D-06 mode-specific fulfillment chain for the order's fulfillment mode.
    - Manual unpaid fallback writes the dedicated override audit payload from Plan 06-01.
    - Terminal orders remain immutable per D-09.
  </acceptance_criteria>
  <done>Merchant order mutation is fully backend-owned, audited, and covered by targeted tests, with index support for grouped order queries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merchant miniapp -> order Cloud Functions | Untrusted merchant actions attempt to read or mutate privileged order data |
| order Cloud Functions -> orders collection | Status and manual settlement writes cross the audited transaction boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | I/E | `queryMerchantOrders` / `getMerchantOrderDetail` | mitigate | Gate every handler with `assertMerchantAccess` and return merchant-safe projections only. |
| T-06-04-02 | T/R | `updateMerchantOrderStatus` | mitigate | Validate transitions through shared rules, require audit fields for manual fallback, and reject all writes after terminal states. |
</threat_model>

<verification>
- `pnpm --filter @xiaipet/cloud-functions test -- queryMerchantOrders getMerchantOrderDetail`
- `pnpm --filter @xiaipet/cloud-functions test -- updateMerchantOrderStatus`
</verification>

<success_criteria>
- Merchant order list/detail/status operations run only through tested backend handlers with D-05 mode-specific paid-state enforcement.
- Manual settlement audit and terminal locks are enforced in backend code, not page logic.
</success_criteria>

<output>
After completion, create `.planning/phases/06-merchant-operations-and-runtime-config/06-04-SUMMARY.md`
</output>
