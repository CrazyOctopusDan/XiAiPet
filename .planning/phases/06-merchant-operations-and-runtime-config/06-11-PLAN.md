---
phase: 06-merchant-operations-and-runtime-config
plan: 11
type: execute
wave: 3
depends_on:
  - 06-06
files_modified:
  - apps/customer-miniapp/src/services/runtime-config.ts
  - apps/customer-miniapp/src/services/runtime-config.test.ts
  - apps/customer-miniapp/src/services/balance.ts
  - apps/customer-miniapp/src/services/balance.test.ts
  - apps/customer-miniapp/pages/home/index.ts
  - apps/customer-miniapp/pages/home/index.wxml
  - apps/customer-miniapp/pages/checkout/index.ts
  - apps/customer-miniapp/pages/checkout/index.wxml
  - apps/customer-miniapp/pages/balance/index.ts
  - apps/customer-miniapp/pages/balance/index.wxml
  - apps/customer-miniapp/pages/cart-checkout.test.ts
autonomous: true
requirements:
  - MUSR-02
  - OPS-01
user_setup: []
must_haves:
  truths:
    - Customer home and checkout consume saved banner, store, notice, and delivery-rule data through the customer-safe `readRuntimeConfig` path from 06-06 instead of hardcoded placeholders or merchant admin handlers.
    - Customer balance history renders merchant adjustments as D-25 normalized titles plus short notes, not raw backend titles or full internal remarks.
  artifacts:
    - path: apps/customer-miniapp/src/services/runtime-config.ts
      provides: Customer runtime config reader
    - path: apps/customer-miniapp/src/services/balance.ts
      provides: Customer balance ledger mapper with D-25 merchant-adjustment presentation rules
    - path: apps/customer-miniapp/pages/home/index.wxml
      provides: Customer home banner rendering sourced from runtime config
    - path: apps/customer-miniapp/pages/checkout/index.wxml
      provides: Checkout store / notice / delivery copy sourced from runtime config
    - path: apps/customer-miniapp/pages/balance/index.wxml
      provides: Customer balance ledger rendering with normalized title and short note
  key_links:
    - from: apps/customer-miniapp/src/services/runtime-config.ts
      to: apps/cloud-functions/src/readRuntimeConfig/index.ts
      via: storefront runtime-config read path
      pattern: "readRuntimeConfig"
    - from: apps/customer-miniapp/src/services/balance.ts
      to: apps/cloud-functions/src/shared/payment-store.ts
      via: merchant-adjustment ledger normalization for customer display
      pattern: "normalizedTitle|shortNote"
---

<objective>
Wire saved runtime config and merchant-adjustment ledger presentation into customer surfaces as a separate Phase 06 slice.

Purpose: Keep customer consumption under the file-count threshold while making OPS-01 outputs and D-25 ledger copy reach real user-facing pages.
Output: Customer runtime-config reader, storefront wiring, and customer balance ledger presentation updates for merchant adjustments.
</objective>

<execution_context>
@/Users/zhangyi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangyi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-merchant-operations-and-runtime-config/06-CONTEXT.md
@.planning/phases/06-merchant-operations-and-runtime-config/06-06-PLAN.md
@packages/shared/src/types/runtime-config.ts
@apps/cloud-functions/src/readRuntimeConfig/index.ts
@apps/cloud-functions/src/adjustUserBalance/index.ts
@apps/cloud-functions/src/shared/payment-store.ts
@apps/customer-miniapp/pages/home/index.ts
@apps/customer-miniapp/pages/checkout/index.ts
@apps/customer-miniapp/src/services/balance.ts
@apps/customer-miniapp/pages/balance/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire saved runtime config into customer home and checkout surfaces</name>
  <files>apps/customer-miniapp/src/services/runtime-config.ts, apps/customer-miniapp/src/services/runtime-config.test.ts, apps/customer-miniapp/pages/home/index.ts, apps/customer-miniapp/pages/home/index.wxml, apps/customer-miniapp/pages/checkout/index.ts, apps/customer-miniapp/pages/checkout/index.wxml, apps/customer-miniapp/pages/cart-checkout.test.ts</files>
  <behavior>
    - Test 1: customer home reads the saved single-banner config through `readRuntimeConfig` rather than a hardcoded asset path or the merchant admin config handler.
    - Test 2: checkout reads store profile, custom notice, and delivery-rule copy from the customer-safe runtime config projection.
    - Test 3: banner resolution uses durable `fileID`-backed data and checkout copy respects disabled custom notice state.
  </behavior>
  <action>Add a customer runtime-config reader service that calls the separate customer-safe `readRuntimeConfig` path introduced in 06-06, then update home plus checkout so banner, store map information, custom notice, and delivery-rule messaging flow from the saved section docs rather than hardcoded placeholders. Do not route storefront reads through `getRuntimeConfigSections`; preserve the customer-side checkout and map interaction boundaries already established in earlier phases, and only replace runtime data sources with backend-owned reads.</action>
  <verify>
    <automated>pnpm --filter @xiaipet/customer-miniapp test -- runtime-config cart-checkout && rg -n "readRuntimeConfig" apps/customer-miniapp/src/services/runtime-config.ts && ! rg -n "getRuntimeConfigSections" apps/customer-miniapp/src/services/runtime-config.ts</automated>
  </verify>
  <acceptance_criteria>
    - Home and checkout consume the saved runtime config output through the customer-safe `readRuntimeConfig` path from 06-06 rather than hardcoded data.
    - Storefront runtime-config reads do not reuse the merchant-only `getRuntimeConfigSections` handler.
    - Checkout consumes saved store profile, custom notice, and delivery-rule copy rather than hardcoded strings.
    - Banner resolution stays `fileID`-backed and custom notice display respects the enabled/disabled state.
  </acceptance_criteria>
  <done>OPS-01 runtime config now reaches customer-visible home and checkout surfaces through a shared customer reader service backed by `readRuntimeConfig`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render merchant balance adjustments with D-25 normalized title and short note</name>
  <files>apps/customer-miniapp/src/services/balance.ts, apps/customer-miniapp/src/services/balance.test.ts, apps/customer-miniapp/pages/balance/index.ts, apps/customer-miniapp/pages/balance/index.wxml</files>
  <behavior>
    - Test 1: customer balance service maps merchant adjustment rows to backend-provided D-25 `normalizedTitle` plus `shortNote`.
    - Test 2: customer balance page renders the normalized title plus short note without exposing the full internal audit remark.
  </behavior>
  <action>Extend the existing balance service and page so merchant adjustment ledger entries consume the backend D-25 fields from 06-06 and render them as `normalizedTitle + shortNote`, rather than raw backend titles or full internal remarks. Keep the rest of the balance-page grouping and interaction model unchanged.</action>
  <verify>
    <automated>pnpm --filter @xiaipet/customer-miniapp test -- balance && rg -n "shortNote|normalizedTitle|ledger-note" apps/customer-miniapp/src/services/balance.ts apps/customer-miniapp/pages/balance/index.wxml</automated>
  </verify>
  <acceptance_criteria>
    - Customer balance service maps merchant adjustments to D-25 `normalizedTitle` plus `shortNote`.
    - Customer balance page renders the normalized title plus short note for merchant operations.
    - The customer surface does not expose the full internal audit remark.
  </acceptance_criteria>
  <done>D-25 is explicitly wired through the customer balance service, page, and tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| customer pages -> runtime-config reader | Admin-entered runtime data becomes customer-visible banner, notice, and checkout copy |
| customer balance page -> ledger mapper | Backend financial history is transformed into a customer-safe presentation model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11-01 | I | `apps/customer-miniapp/src/services/runtime-config.ts` | mitigate | Consume only the customer-safe `readRuntimeConfig` projection from 06-06 and resolve banner assets from durable `fileID`-backed records. |
| T-06-11-02 | I | `apps/customer-miniapp/src/services/balance.ts` / `pages/balance/index.wxml` | mitigate | Render merchant adjustments only from backend D-25 `normalizedTitle` and `shortNote` fields so the user surface never displays the full internal operator remark. |
</threat_model>

<verification>
- `pnpm --filter @xiaipet/customer-miniapp test -- runtime-config cart-checkout`
- `rg -n "readRuntimeConfig" apps/customer-miniapp/src/services/runtime-config.ts`
- `! rg -n "getRuntimeConfigSections" apps/customer-miniapp/src/services/runtime-config.ts`
- `pnpm --filter @xiaipet/customer-miniapp test -- balance`
- `rg -n "shortNote|normalizedTitle|ledger-note" apps/customer-miniapp/src/services/balance.ts apps/customer-miniapp/pages/balance/index.wxml`
</verification>

<success_criteria>
- Customer home and checkout actually consume the saved runtime config outputs through the separate customer-safe `readRuntimeConfig` path from 06-06.
- D-25 merchant-adjustment rendering is explicitly planned on the customer balance service/page path, using normalized title plus short note.
</success_criteria>

<output>
After completion, create `.planning/phases/06-merchant-operations-and-runtime-config/06-11-SUMMARY.md`
</output>
