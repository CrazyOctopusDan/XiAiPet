---
phase: 06-merchant-operations-and-runtime-config
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/catalog-admin.ts
  - packages/shared/src/schema/catalog-admin.ts
  - packages/shared/src/schema/catalog-admin.test.ts
  - packages/shared/src/rules/product-pricing.ts
  - packages/shared/src/rules/product-pricing.test.ts
autonomous: true
requirements:
  - MCAT-01
  - MPRD-01
  - MPRD-02
user_setup: []
must_haves:
  truths:
    - Merchant category contracts explicitly include both category name and short text/emoji icon token per MCAT-01 and the resolved research decision.
    - Product admin contracts enforce a single category, the fixed three-step editor shape, product image fields backed by CloudBase storage `fileID` persistence, purchase-limit fields, long-form product detail content, and auto-summed pricing with explicit override rows per D-12 through D-17, MPRD-01, and MPRD-02.
  artifacts:
    - path: packages/shared/src/types/catalog-admin.ts
      provides: Category/product admin record shapes including `iconToken`, product image `fileID` fields, `purchaseLimit`, and detail-content fields
    - path: packages/shared/src/schema/catalog-admin.ts
      provides: Category/product validation rules for backend handlers and forms, including fileID-backed image persistence
    - path: packages/shared/src/rules/product-pricing.ts
      provides: Derived pricing helper for base + spec + formula math and override cases
  key_links:
    - from: packages/shared/src/schema/catalog-admin.ts
      to: future category/product cloud functions
      via: shared validation contract
      pattern: "iconToken|imageFileId|categoryId|purchaseLimit|detailContent|fulfillmentModes"
    - from: packages/shared/src/rules/product-pricing.ts
      to: future product upsert handler
      via: shared pricing computation
      pattern: "override|formula|spec"
---

<objective>
Define the contract layer for merchant categories and products before CloudBase handlers or merchant forms are built.

Purpose: Make MCAT-01 icon handling explicit end-to-end and keep product pricing logic testable outside page code.
Output: Shared category/product types, schemas, and pricing helpers with focused tests.
</objective>

<execution_context>
@/Users/zhangyi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangyi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-merchant-operations-and-runtime-config/06-CONTEXT.md
@.planning/phases/06-merchant-operations-and-runtime-config/06-RESEARCH.md
@.planning/phases/06-merchant-operations-and-runtime-config/06-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define category and product admin schemas with explicit icon-token support</name>
  <files>packages/shared/src/types/catalog-admin.ts, packages/shared/src/schema/catalog-admin.ts, packages/shared/src/schema/catalog-admin.test.ts</files>
  <read_first>
    - /Users/zhangyi/zhangyi/homework/xiaipet/.planning/phases/06-merchant-operations-and-runtime-config/06-CONTEXT.md
    - /Users/zhangyi/zhangyi/homework/xiaipet/.planning/phases/06-merchant-operations-and-runtime-config/06-RESEARCH.md
    - /Users/zhangyi/zhangyi/homework/xiaipet/.planning/phases/06-merchant-operations-and-runtime-config/06-UI-SPEC.md
    - /Users/zhangyi/zhangyi/homework/xiaipet/.planning/REQUIREMENTS.md
  </read_first>
  <behavior>
    - Test 1: category records require both `name` and a short text/emoji `iconToken` for MCAT-01.
    - Test 2: product records allow exactly one `categoryId` per D-12 and follow the three-step editor data shape from D-14.
    - Test 3: product records require a storage-backed image field such as `imageFileId`, and the schema rejects temp URLs or page-local paths as the persisted source of truth for MPRD-01.
    - Test 4: product records require explicit `purchaseLimit` and long-form product detail content fields so MPRD-02 cannot be reduced to pricing-only editing.
    - Test 5: category delete preflight shape exposes linked-product counts so backend delete guards can enforce D-18.
  </behavior>
  <action>Create shared category and product admin types plus validation schemas. Lock category icon representation to a short text/emoji token for Phase 6 unless a separate customer asset migration is added. Make the deletion preflight and product editor payloads explicit so later backend handlers and merchant forms cannot skip icon/name/category rules. For MPRD-01, add a first-class product image persistence contract that stores the CloudBase storage result as a canonical `imageFileId` field, optionally carries display-only preview metadata separately, and explicitly forbids persisting temp URLs, local temp paths, or raw file blobs as the saved product image reference. Keep `purchaseLimit` plus product detail content fields as first-class contract fields for MPRD-02 rather than optional page-only metadata.</action>
  <verify>
    <automated>pnpm --filter @xiaipet/shared test -- catalog-admin && rg -n "imageFileId|purchaseLimit|detailContent" packages/shared/src/types/catalog-admin.ts packages/shared/src/schema/catalog-admin.ts</automated>
  </verify>
  <acceptance_criteria>
    - Shared category/product contracts explicitly include a CloudBase storage-backed product image `fileID` field for MPRD-01.
    - Validation rules make `imageFileId` the persisted image source of truth instead of temp URLs or local upload paths.
    - The same schema still preserves single-category assignment, purchase limits, detail content, and delete-preflight metadata.
  </acceptance_criteria>
  <done>Shared schemas now cover category name + icon token, single-category products, storage-backed product image fileID persistence, purchase-limit/detail-content fields, delete preflight metadata, and the three-step editor payload shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Codify product pricing and override rules outside the merchant editor UI</name>
  <files>packages/shared/src/rules/product-pricing.ts, packages/shared/src/rules/product-pricing.test.ts</files>
  <read_first>
    - /Users/zhangyi/zhangyi/homework/xiaipet/.planning/phases/06-merchant-operations-and-runtime-config/06-02-PLAN.md
    - /Users/zhangyi/zhangyi/homework/xiaipet/packages/shared/src/types/catalog-admin.ts
    - /Users/zhangyi/zhangyi/homework/xiaipet/packages/shared/src/schema/catalog-admin.ts
  </read_first>
  <behavior>
    - Test 1: default combination price equals `base + spec surcharge + formula surcharge` per D-17.
    - Test 2: explicit override rows are allowed but remain sparse, targeted exceptions.
    - Test 3: no product save path can omit fulfillment-mode, stock-sensitive publish metadata, or the image-backed base-info contract required by MPRD-01.
  </behavior>
  <action>Implement pure pricing helpers that merchant pages and cloud functions can reuse directly. Keep override rows explicit and narrow instead of replacing the default math path, and expose validation-friendly outputs that later upsert handlers can enforce without duplicating calculations in page code.</action>
  <verify>
    <automated>pnpm --filter @xiaipet/shared test -- product-pricing</automated>
  </verify>
  <acceptance_criteria>
    - Pricing helpers remain independent from page code and reusable by the product save handler.
    - The rule layer is documented against a product contract that already includes required image/base-info fields from MPRD-01.
  </acceptance_criteria>
  <done>Product pricing rules are shared, testable, and ready to power backend validation plus merchant editor summaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merchant form payload -> shared schema | Untrusted admin payloads enter category/product validation before persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | T | `packages/shared/src/schema/catalog-admin.ts` | mitigate | Require category `name` and `iconToken`, single `categoryId`, `purchaseLimit`, product detail content, and delete-preflight metadata so later handlers can reject malformed admin writes. |
| T-06-02-02 | T/R | `packages/shared/src/rules/product-pricing.ts` | mitigate | Centralize auto-sum and override math in one tested helper so merchant UI and backend handlers cannot diverge on pricing outcomes. |
</threat_model>

<verification>
- `pnpm --filter @xiaipet/shared test -- catalog-admin && rg -n "imageFileId|purchaseLimit|detailContent" packages/shared/src/types/catalog-admin.ts packages/shared/src/schema/catalog-admin.ts`
- `pnpm --filter @xiaipet/shared test -- product-pricing`
</verification>

<success_criteria>
- MCAT-01 icon handling is explicit in shared types and validation.
- Product admin data shapes now explicitly include CloudBase storage-backed product image fileID persistence for MPRD-01 plus purchase-limit and product-detail-content management for MPRD-02.
- Pricing rules remain stable enough for backend and UI plans to consume directly without redefining the product image/base-info contract.
</success_criteria>

<output>
After completion, create `.planning/phases/06-merchant-operations-and-runtime-config/06-02-SUMMARY.md`
</output>
