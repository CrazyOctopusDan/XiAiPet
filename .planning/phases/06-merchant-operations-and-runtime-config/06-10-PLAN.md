---
phase: 06-merchant-operations-and-runtime-config
plan: 10
type: execute
wave: 3
depends_on:
  - 06-03
  - 06-06
files_modified:
  - apps/merchant-miniapp/pages/runtime-config/index.ts
  - apps/merchant-miniapp/pages/runtime-config/index.wxml
  - apps/merchant-miniapp/pages/runtime-config/index.wxss
  - apps/merchant-miniapp/pages/runtime-config/index.json
  - apps/merchant-miniapp/src/services/runtime-config-admin.ts
  - apps/merchant-miniapp/src/services/runtime-config-admin.test.ts
autonomous: true
requirements:
  - OPS-01
user_setup: []
must_haves:
  truths:
    - Merchant can manage runtime config in one entry page with independently saved sections per D-27.
    - Membership tiers in runtime config capture threshold, name, and description together per D-29.
    - Delivery-fee rules are edited as visible explainer rows using the locked tier copy per D-32 and D-33, not hidden formula-only config.
  artifacts:
    - path: apps/merchant-miniapp/src/services/runtime-config-admin.ts
      provides: Merchant runtime-config service layer
    - path: apps/merchant-miniapp/pages/runtime-config/index.ts
      provides: Sectioned merchant runtime config page with D-29 membership fields and D-32 delivery-fee explainer editing
  key_links:
    - from: apps/merchant-miniapp/src/services/runtime-config-admin.ts
      to: upsertRuntimeConfigSection
      via: section-level save flow
      pattern: "upsertRuntimeConfigSection"
    - from: apps/merchant-miniapp/pages/runtime-config/index.ts
      to: apps/merchant-miniapp/src/services/runtime-config-admin.ts
      via: per-section save and unsaved-state flow
      pattern: "membership|delivery|saveSection|dirty"
---

<objective>
Build the merchant runtime-config admin surface as its own small Phase 06 slice.

Purpose: Finish the merchant-facing half of OPS-01 with explicit section editing instead of overloading one large mixed merchant/customer plan.
Output: Merchant runtime-config page and service with D-29 membership editing and D-32 delivery-fee explainer editing.
</objective>

<execution_context>
@/Users/zhangyi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangyi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-merchant-operations-and-runtime-config/06-CONTEXT.md
@.planning/phases/06-merchant-operations-and-runtime-config/06-UI-SPEC.md
@packages/shared/src/types/runtime-config.ts
@apps/cloud-functions/src/upsertRuntimeConfigSection/index.ts
@apps/cloud-functions/src/getRuntimeConfigSections/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the merchant runtime-config admin service with explicit section payload shaping</name>
  <files>apps/merchant-miniapp/src/services/runtime-config-admin.ts, apps/merchant-miniapp/src/services/runtime-config-admin.test.ts</files>
  <action>Create a runtime-config admin service that reads and saves sections independently through Cloud Functions. The service contract must preserve the five locked sections, map membership tiers as D-29 `threshold + name + description` tuples, and expose delivery-fee data as D-32 explainer rows using the exact D-33 locked copy instead of generic JSON blobs.</action>
  <verify>
    <automated>pnpm --filter @xiaipet/merchant-miniapp test -- runtime-config-admin</automated>
  </verify>
  <acceptance_criteria>
    - The admin service preserves independent section reads and writes per D-27.
    - Membership tiers are shaped as D-29 threshold, name, and description fields.
    - Delivery fees are exposed as D-32 explainer rows with the exact D-33 locked copy.
  </acceptance_criteria>
  <done>The merchant runtime-config service exposes section-shaped payloads for the locked sections, D-29 tier fields, and D-32 delivery-fee explainer rows.</done>
</task>

<task type="auto">
  <name>Task 2: Build the merchant runtime-config page with section saves and explicit delivery-fee explainer UI</name>
  <files>apps/merchant-miniapp/pages/runtime-config/index.ts, apps/merchant-miniapp/pages/runtime-config/index.wxml, apps/merchant-miniapp/pages/runtime-config/index.wxss, apps/merchant-miniapp/pages/runtime-config/index.json</files>
  <action>Build the merchant runtime-config page as one entry surface with the five locked sections, per-section save buttons, and unsaved badges. Make D-29 explicit in the membership section by rendering threshold, tier name, and tier description fields together. Make D-32 explicit in the delivery section by presenting the D-33 rows as a visible explainer/editing model rather than abstract algorithm controls or hidden JSON.</action>
  <verify>
    <automated>rg -n "店铺信息|配送费规则|会员等级|首页 Banner|定制提示|未保存|等级说明|累计消费门槛" apps/merchant-miniapp/pages/runtime-config/index.wxml && rg -n "5\\.0 公里内 98 元起送，配送费 0 元|10\\.0 公里内 98 元起送，配送费 15 元|说明弹层|配送费说明" apps/merchant-miniapp/pages/runtime-config/index.wxml apps/merchant-miniapp/pages/runtime-config/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - The runtime-config page remains a single entry surface with independently saveable sections per D-27.
    - Membership editing exposes D-29 threshold, name, and description fields together.
    - Delivery-fee editing uses the D-32 explainer model and the exact D-33 locked copy rather than hidden algorithm-only controls.
  </acceptance_criteria>
  <done>The merchant runtime-config page supports independent section saves and preserves the locked section inventory, D-29 tier fields, and D-32 delivery-fee explainer content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merchant runtime page -> runtime-config service | Untrusted admin config payloads are sent to privileged save handlers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10-01 | T | `apps/merchant-miniapp/pages/runtime-config/index.ts` | mitigate | Keep config editing section-scoped with explicit save boundaries and schema-backed service calls. |
| T-06-10-02 | T/I | `apps/merchant-miniapp/src/services/runtime-config-admin.ts` | mitigate | Preserve D-29 membership payload fields and D-32 delivery-fee explainer rows in the service layer so the page cannot drift into opaque JSON-only editing. |
</threat_model>

<verification>
- `pnpm --filter @xiaipet/merchant-miniapp test -- runtime-config-admin`
- `rg -n "店铺信息|配送费规则|会员等级|首页 Banner|定制提示|未保存" apps/merchant-miniapp/pages/runtime-config/index.wxml`
- `rg -n "5\\.0 公里内 98 元起送，配送费 0 元|10\\.0 公里内 98 元起送，配送费 15 元|说明弹层|配送费说明" apps/merchant-miniapp/pages/runtime-config/index.wxml apps/merchant-miniapp/pages/runtime-config/index.ts`
</verification>

<success_criteria>
- OPS-01 runtime config editing is independently saveable in merchant UI.
- D-29 membership tier fields are explicit in the merchant admin surface.
- D-32 delivery-fee explainer rows are explicit in the merchant admin surface and keep the locked D-33 copy visible.
</success_criteria>

<output>
After completion, create `.planning/phases/06-merchant-operations-and-runtime-config/06-10-SUMMARY.md`
</output>
