---
phase: 06-merchant-operations-and-runtime-config
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/merchant-miniapp/package.json
  - apps/merchant-miniapp/vitest.config.ts
  - apps/merchant-miniapp/src/testing/harness-smoke.test.ts
  - packages/shared/src/types/order.ts
  - packages/shared/src/rules/order-fulfillment.ts
  - packages/shared/src/rules/order-fulfillment.test.ts
  - apps/customer-miniapp/src/services/orders.ts
  - apps/customer-miniapp/src/services/orders.test.ts
autonomous: true
requirements:
  - MORD-01
  - MORD-02
user_setup: []
must_haves:
  truths:
    - Paid orders use fulfillment-mode-specific fulfillment chains per D-05 and D-06 instead of one shared minimal paid-status model.
    - Merchant and customer surfaces use the same detailed fulfillment labels per D-05, D-06, and D-10.
    - Manual merchant payment fallback is modeled as a separate audited override record and does not overwrite checkout payment selection per D-07 and D-08.
  artifacts:
    - path: packages/shared/src/types/order.ts
      provides: Payment-safe fulfillment and manual settlement contract
    - path: packages/shared/src/rules/order-fulfillment.ts
      provides: Shared per-mode paid-state labels, grouping, and transition helpers
    - path: apps/customer-miniapp/src/services/orders.ts
      provides: Customer order label parity with merchant fulfillment states
    - path: apps/merchant-miniapp/vitest.config.ts
      provides: Merchant miniapp Wave 0 test harness
  key_links:
    - from: packages/shared/src/rules/order-fulfillment.ts
      to: apps/customer-miniapp/src/services/orders.ts
      via: shared status label helper
      pattern: "order-fulfillment|statusLabel"
    - from: packages/shared/src/types/order.ts
      to: future merchant order handlers
      via: manual settlement / override subrecord
      pattern: "manualSettlement|merchantOverride"
---

<objective>
Stabilize the shared merchant-order contract before any Phase 06 order handlers or pages land.

Purpose: Preserve Phase 5 payment semantics while creating a fast merchant-miniapp test harness and shared fulfillment rules.
Output: Fulfillment contract/rules, customer label parity updates, and merchant miniapp Vitest bootstrap.
</objective>

<execution_context>
@/Users/zhangyi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhangyi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-merchant-operations-and-runtime-config/06-CONTEXT.md
@.planning/phases/06-merchant-operations-and-runtime-config/06-RESEARCH.md
@.planning/phases/05-checkout-payment-and-orders/05-04-SUMMARY.md
@packages/shared/src/types/order.ts
@apps/customer-miniapp/src/services/orders.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define fulfillment state and manual settlement audit without mutating checkout payment choice</name>
  <files>packages/shared/src/types/order.ts, packages/shared/src/rules/order-fulfillment.ts, packages/shared/src/rules/order-fulfillment.test.ts</files>
  <behavior>
    - Test 1: `delivery`, `pickup`, and `express` orders map to the exact D-06 fulfillment chains and merchant grouping labels.
    - Test 2: unpaid manual fallback stores adjustment method, reason note, operator metadata, and before/after status in a dedicated subrecord per D-07 and D-08.
    - Test 3: terminal fulfillment states remain immutable once entered per D-09.
  </behavior>
  <action>Expand the shared order contract to add a fulfillment-state object plus manual settlement / merchant override audit data, not a rewritten top-level `paymentMethod`, per the resolved research decision. Implement shared grouping, label, and transition helpers that encode D-05 and D-06 explicitly: paid orders must follow fulfillment-mode-specific chains for `delivery`, `pickup`, and `express`, not one shared minimal paid-status model.</action>
  <verify>
    <automated>pnpm --filter @xiaipet/shared test -- order-fulfillment</automated>
  </verify>
  <acceptance_criteria>
    - Shared order types separate payment and fulfillment concerns while encoding D-05 mode-specific paid-state chains.
    - Manual fallback remains a dedicated audited override record and does not rewrite checkout payment selection.
    - Transition helpers expose the exact D-06 labels for both merchant and customer consumers.
  </acceptance_criteria>
  <done>Shared order types separate payment and fulfillment concerns, and the fulfillment rule tests prove valid chains, manual fallback audit requirements, and terminal-state locks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bootstrap merchant miniapp tests and align customer order labels to the shared helper</name>
  <files>apps/merchant-miniapp/package.json, apps/merchant-miniapp/vitest.config.ts, apps/merchant-miniapp/src/testing/harness-smoke.test.ts, apps/customer-miniapp/src/services/orders.ts, apps/customer-miniapp/src/services/orders.test.ts</files>
  <behavior>
    - Test 1: merchant miniapp has a runnable targeted Vitest command for later Phase 06 UI slices.
    - Test 2: customer order cards/details render the same detailed fulfillment text as merchant pages per D-05, D-06, and D-10.
  </behavior>
  <action>Replace the merchant-miniapp placeholder test script with a real targeted Vitest runner and add one smoke test so later UI plans can verify quickly. Update the customer order service and its tests to consume the shared fulfillment helper instead of keeping a separate simplified status taxonomy, so D-05 mode-specific paid-state flows and D-10 customer copy parity stay locked together.</action>
  <verify>
    <automated>pnpm --filter @xiaipet/merchant-miniapp test -- harness-smoke && pnpm --filter @xiaipet/customer-miniapp test -- orders</automated>
  </verify>
  <acceptance_criteria>
    - Merchant miniapp has a runnable targeted test harness for later Phase 06 UI slices.
    - Customer order mapping consumes the shared D-05 and D-06 fulfillment helper rather than a page-local simplified status model.
  </acceptance_criteria>
  <done>Merchant miniapp supports focused test runs, and customer order status mapping is fully shared with the merchant fulfillment contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared contract -> merchant/customer clients | Fulfillment labels and manual settlement semantics cross from backend-owned state into UI rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | T/R | `packages/shared/src/types/order.ts` | mitigate | Keep merchant override data in a dedicated audited subrecord so payment selection remains intact and reviewable. |
| T-06-01-02 | T/I | `packages/shared/src/rules/order-fulfillment.ts` | mitigate | Centralize labels and transitions in one helper consumed by both merchant and customer services to prevent divergent status copy. |
</threat_model>

<verification>
- `pnpm --filter @xiaipet/shared test -- order-fulfillment`
- `pnpm --filter @xiaipet/merchant-miniapp test -- harness-smoke`
- `pnpm --filter @xiaipet/customer-miniapp test -- orders`
</verification>

<success_criteria>
- Shared order contracts preserve Phase 5 payment fields while adding D-05 fulfillment-mode-specific merchant governance.
- Customer order views no longer maintain a separate simplified status mapping.
- Merchant miniapp has a fast test harness ready for later UI plans.
</success_criteria>

<output>
After completion, create `.planning/phases/06-merchant-operations-and-runtime-config/06-01-SUMMARY.md`
</output>
