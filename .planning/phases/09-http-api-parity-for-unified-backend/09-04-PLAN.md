---
phase: 9
plan: 09-04
name: Merchant access and order management APIs
type: execute
wave: 4
depends_on:
  - 09-01
  - 09-03
files_modified:
  - apps/api/src/modules/auth/guards.ts
  - apps/api/src/modules/orders/repository.ts
  - apps/api/src/modules/orders/service.ts
  - apps/api/src/routes/api-v1.ts
  - apps/api/src/routes/merchant/orders.ts
  - apps/api/src/routes/api-parity.ts
  - apps/api/src/routes/merchant-orders.routes.test.ts
autonomous: true
requirements:
  - API-05
  - API-06
must_haves:
  truths:
    - Every merchant order route rejects non-whitelisted sessions before sensitive order repository/service access.
    - Merchant status updates preserve existing transition, fulfillment and audit rules.
    - Unauthorized users must not learn whether a merchant order exists.
  artifacts:
    - path: apps/api/src/routes/merchant/orders.ts
      provides: Merchant order list/detail/status HTTP routes
    - path: apps/api/src/modules/orders/service.ts
      provides: Merchant order query and status transition orchestration
    - path: apps/api/src/routes/merchant-orders.routes.test.ts
      provides: Early rejection and transition coverage
  key_links:
    - from: apps/api/src/routes/merchant/orders.ts
      to: apps/api/src/modules/auth/guards.ts
      via: merchant pre-handler
      pattern: "requireMerchantSession"
---

<objective>
Expose merchant order list, detail, and status update APIs with strict merchant whitelist authorization.
</objective>

<threat_model>
T9-04: Non-whitelisted sessions access merchant order data or infer order existence.
Severity: high.
Mitigation: All routes use `requireMerchantSession`; tests assert fake order service methods are not called on forbidden access.
</threat_model>

<tasks>
  <task id="09-04-01" type="standard">
    <name>Extend merchant order service methods</name>
    <files>
      <file>apps/api/src/modules/orders/repository.ts</file>
      <file>apps/api/src/modules/orders/service.ts</file>
    </files>
    <read_first>
      <file>apps/api/src/modules/orders/repository.ts</file>
      <file>apps/api/src/modules/orders/service.ts</file>
      <file>apps/cloud-functions/src/queryMerchantOrders/index.ts</file>
      <file>apps/cloud-functions/src/getMerchantOrderDetail/index.ts</file>
      <file>apps/cloud-functions/src/updateMerchantOrderStatus/index.ts</file>
      <file>packages/shared/src/types/order.ts</file>
      <file>packages/shared/src/rules/order-fulfillment.ts</file>
    </read_first>
    <action>
      Add `queryMerchantOrders(merchantContext, filters)`, `getMerchantOrderDetail(merchantContext, orderId)`, and `updateMerchantOrderStatus(merchantContext, orderId, payload)` to the order service. Preserve terminal order lock behavior, manual settlement requirement when marking paid, fulfillment transition validation, cancel behavior, and merchant timeline/audit entry with actor identity from `merchantContext`.
    </action>
    <verify>
      <automated>rg "queryMerchantOrders|getMerchantOrderDetail|updateMerchantOrderStatus|merchantTimeline|terminal" apps/api/src/modules/orders</automated>
    </verify>
    <acceptance_criteria>
      `apps/api/src/modules/orders/service.ts` contains all three merchant methods and writes or preserves a merchant timeline/audit field during status update.
    </acceptance_criteria>
    <done>Merchant order service methods exist.</done>
  </task>

  <task id="09-04-02" type="standard">
    <name>Register merchant order routes</name>
    <files>
      <file>apps/api/src/routes/api-v1.ts</file>
      <file>apps/api/src/routes/merchant/orders.ts</file>
      <file>apps/api/src/routes/api-parity.ts</file>
    </files>
    <read_first>
      <file>apps/api/src/routes/api-v1.ts</file>
      <file>apps/api/src/modules/auth/guards.ts</file>
      <file>apps/api/src/modules/orders/service.ts</file>
      <file>apps/api/src/routes/api-parity.ts</file>
    </read_first>
    <action>
      Add routes `GET /api/v1/merchant/orders`, `GET /api/v1/merchant/orders/:orderId`, and `PATCH /api/v1/merchant/orders/:orderId/status`. All use `requireMerchantSession`. Successful responses are `{ ok: true, orders }` for list and `{ ok: true, order }` for detail/update. Add parity mappings for `queryMerchantOrders`, `getMerchantOrderDetail`, and `updateMerchantOrderStatus`.
    </action>
    <verify>
      <automated>rg "/api/v1/merchant/orders|queryMerchantOrders|getMerchantOrderDetail|updateMerchantOrderStatus" apps/api/src/routes</automated>
    </verify>
    <acceptance_criteria>
      `apps/api/src/routes/merchant/orders.ts` contains all three route paths; `api-parity.ts` contains all three CloudBase function names.
    </acceptance_criteria>
    <done>Merchant order routes and mappings exist.</done>
  </task>

  <task id="09-04-03" type="standard">
    <name>Add merchant order route tests</name>
    <files>
      <file>apps/api/src/routes/merchant-orders.routes.test.ts</file>
    </files>
    <read_first>
      <file>apps/api/src/routes/merchant/orders.ts</file>
      <file>apps/api/src/modules/auth/guards.ts</file>
      <file>apps/api/src/modules/orders/service.ts</file>
    </read_first>
    <action>
      Add Fastify inject tests covering forbidden access before service call, order list success, order detail success, invalid status transition failure, terminal order protection, and successful status update with audit actor. Use fake services/providers; do not require MySQL.
    </action>
    <verify>
      <automated>pnpm --filter @xiaipet/api test -- src/routes/merchant-orders.routes.test.ts</automated>
    </verify>
    <acceptance_criteria>
      `apps/api/src/routes/merchant-orders.routes.test.ts` asserts the fake order service is not called when merchant access is denied and includes tests for list, detail, invalid transition, terminal protection, and status update success.
    </acceptance_criteria>
    <done>Merchant order route tests cover auth and transition behavior.</done>
  </task>
</tasks>

<verification>
Run:
- `pnpm --filter @xiaipet/api typecheck`
- `pnpm --filter @xiaipet/api test -- src/routes/merchant-orders.routes.test.ts`
- `rg "queryMerchantOrders|getMerchantOrderDetail|updateMerchantOrderStatus" apps/api/src/routes/api-parity.ts`
</verification>

<success_criteria>
- Merchant order APIs are registered and protected by merchant guard.
- Tests prove forbidden access happens before sensitive order service calls.
- Status transition behavior preserves existing CloudBase rules and audit trail semantics.
</success_criteria>
