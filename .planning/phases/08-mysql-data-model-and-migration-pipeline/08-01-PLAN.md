---
phase: 8
plan: 08-01
name: Prisma schema and database config
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/api/.env.example
  - apps/api/prisma.config.ts
  - apps/api/prisma/schema.prisma
  - apps/api/src/config/env.ts
  - apps/api/src/config/env.test.ts
autonomous: true
requirements:
  - DB-01
  - DB-02
must_haves:
  truths:
    - Prisma schema defines User, MerchantUser, Category, Product, RuntimeConfigSection, Order, Payment, BalanceAccount, BalanceLedger and ReceiptPrintAudit.
    - DATABASE_URL is validated without committing real credentials.
    - Order snapshots and runtime config values use JSON fields.
  artifacts:
    - path: apps/api/prisma/schema.prisma
      provides: MySQL source-of-truth schema for Phase 8 data model
    - path: apps/api/prisma.config.ts
      provides: Env-based Prisma CLI configuration
    - path: apps/api/src/config/env.ts
      provides: Runtime validation for DATABASE_URL
    - path: apps/api/.env.example
      provides: Placeholder-only local DATABASE_URL documentation
  key_links:
    - from: apps/api/src/config/env.ts
      to: apps/api/prisma.config.ts
      via: DATABASE_URL
      pattern: "mysql://|mysqls://"
    - from: apps/api/prisma/schema.prisma
      to: packages/shared/src/types/order.ts
      via: order snapshot JSON fields
      pattern: "snapshot Json"
---

<objective>
Add Prisma/MySQL tooling and define the first MySQL schema for users, merchant users, catalog, runtime config, orders, payments, balances, ledgers and receipt print audits.
</objective>

<threat_model>
T8-01: Database credentials or RDS URLs are committed while adding Prisma config.
Severity: high.
Mitigation: `DATABASE_URL` is read from environment only; `.env.example` contains only local placeholder values; real `.env*` files remain ignored.
</threat_model>

<tasks>
  <task id="08-01-01" type="standard">
    <name>Add Prisma package scripts and placeholder database env</name>
    <files>
      <file>apps/api/package.json</file>
      <file>apps/api/.env.example</file>
    </files>
    <read_first>
      <file>apps/api/package.json</file>
      <file>package.json</file>
      <file>apps/api/.env.example</file>
      <file>apps/api/src/config/env.ts</file>
      <file>.planning/phases/08-mysql-data-model-and-migration-pipeline/08-CONTEXT.md</file>
      <file>.planning/phases/08-mysql-data-model-and-migration-pipeline/08-RESEARCH.md</file>
    </read_first>
    <action>
      Add Prisma tooling to `@xiaipet/api`: runtime dependency `@prisma/client`, dev dependencies `prisma` and `tsx`. Add scripts: `db:generate` running `prisma generate --config prisma.config.ts`, `db:migrate:dev` running `prisma migrate dev --config prisma.config.ts`, `db:migrate:deploy` running `prisma migrate deploy --config prisma.config.ts`, `db:seed` running `prisma db seed --config prisma.config.ts`, and `db:verify` running `tsx scripts/verify-data.ts`. Add `DATABASE_URL=mysql://xiaipet:xiaipet_local_password@127.0.0.1:3307/xiaipet_dev` to `apps/api/.env.example`.
    </action>
    <verify>
      <automated>rg "db:generate|db:migrate:dev|db:migrate:deploy|db:seed|db:verify" apps/api/package.json</automated>
    </verify>
    <acceptance_criteria>
      `apps/api/package.json` contains all five `db:*` scripts and `apps/api/.env.example` contains `DATABASE_URL=mysql://xiaipet:xiaipet_local_password@127.0.0.1:3307/xiaipet_dev`.
    </acceptance_criteria>
    <done>Prisma dependencies/scripts and placeholder `DATABASE_URL` are present.</done>
  </task>

  <task id="08-01-02" type="standard">
    <name>Validate DATABASE_URL in API config</name>
    <files>
      <file>apps/api/src/config/env.ts</file>
      <file>apps/api/src/config/env.test.ts</file>
    </files>
    <read_first>
      <file>apps/api/src/config/env.ts</file>
      <file>apps/api/src/config/env.test.ts</file>
    </read_first>
    <action>
      Extend `ApiConfig` with `databaseUrl: string`. In `loadApiConfig`, read `DATABASE_URL`, throw `Invalid DATABASE_URL: expected a MySQL connection string` when it is missing outside `NODE_ENV=test` or when it does not start with `mysql://` or `mysqls://`. In test mode, default to `mysql://xiaipet:xiaipet_local_password@127.0.0.1:3307/xiaipet_test`. Add tests for valid MySQL URL, missing production URL, invalid provider URL, and test default.
    </action>
    <verify>
      <automated>pnpm --filter @xiaipet/api test -- src/config/env.test.ts</automated>
    </verify>
    <acceptance_criteria>
      `apps/api/src/config/env.test.ts` asserts the exact error string `Invalid DATABASE_URL: expected a MySQL connection string` and `loadApiConfig({ NODE_ENV: "test" }).databaseUrl` starts with `mysql://`.
    </acceptance_criteria>
    <done>API config exposes a validated `databaseUrl` value.</done>
  </task>

  <task id="08-01-03" type="standard">
    <name>Define Prisma MySQL schema</name>
    <files>
      <file>apps/api/prisma/schema.prisma</file>
    </files>
    <read_first>
      <file>packages/shared/src/types/order.ts</file>
      <file>packages/shared/src/types/catalog-admin.ts</file>
      <file>packages/shared/src/types/user.ts</file>
      <file>packages/shared/src/types/runtime-config.ts</file>
      <file>packages/shared/src/types/user-admin.ts</file>
      <file>.planning/phases/08-mysql-data-model-and-migration-pipeline/08-PATTERNS.md</file>
    </read_first>
    <action>
      Create `apps/api/prisma/schema.prisma` with `generator client { provider = "prisma-client-js" }` and `datasource db { provider = "mysql" }`. Define enums for `UserStatus`, `PhoneBindingState`, `ProductStatus`, `PaymentMethod`, `OrderStatus`, `OrderPaymentStatus`, `OrderFulfillmentMode`, `OrderFulfillmentStatus`, `PaymentStatus`, `LedgerReason`, and `ReceiptPrintResult`. Define models `User`, `MerchantUser`, `Category`, `Product`, `RuntimeConfigSection`, `Order`, `Payment`, `BalanceAccount`, `BalanceLedger`, and `ReceiptPrintAudit`. Use `Decimal @db.Decimal(10, 2)` for money fields; use `Json` for product specs/formulas/priceOverrides/purchaseLimit/fulfillmentModes, runtime config `value`, order `snapshot`, ledger `metadata`, and audit `operator`. Add unique constraints: `User.openid`, `MerchantUser.openid`, `Category.legacyId`, `Product.legacyId`, `Order.openid_idempotencyKey`, `Payment.orderId`, `BalanceAccount.openid`, and `BalanceLedger.legacyId`.
    </action>
    <verify>
      <automated>rg "model User|model MerchantUser|model Product|model Order|model Payment|model BalanceAccount|model BalanceLedger|model ReceiptPrintAudit|Json|Decimal" apps/api/prisma/schema.prisma</automated>
    </verify>
    <acceptance_criteria>
      `apps/api/prisma/schema.prisma` contains all required model names, contains at least one `@db.Decimal(10, 2)`, and contains `@@unique([openid, idempotencyKey])` on `Order`.
    </acceptance_criteria>
    <done>Prisma schema covers the Phase 8 data model families.</done>
  </task>

  <task id="08-01-04" type="standard">
    <name>Add Prisma config</name>
    <files>
      <file>apps/api/prisma.config.ts</file>
    </files>
    <read_first>
      <file>apps/api/prisma/schema.prisma</file>
      <file>apps/api/.env.example</file>
    </read_first>
    <action>
      Create `apps/api/prisma.config.ts` importing `dotenv/config`, `defineConfig`, and `env` from `prisma/config`. Configure schema path `prisma/schema.prisma`, migrations path `prisma/migrations`, seed command `tsx prisma/seed.ts`, and datasource URL `env("DATABASE_URL")`. Do not hard-code any database password or host in `prisma.config.ts`.
    </action>
    <verify>
      <automated>rg "defineConfig|schema: \"prisma/schema.prisma\"|seed: \"tsx prisma/seed.ts\"|env\\(\"DATABASE_URL\"\\)" apps/api/prisma.config.ts</automated>
    </verify>
    <acceptance_criteria>
      `apps/api/prisma.config.ts` contains `env("DATABASE_URL")` and does not contain `xiaipet_local_password`.
    </acceptance_criteria>
    <done>Prisma CLI resolves schema, migrations and seed through env-based config.</done>
  </task>
</tasks>

<verification>
Run:
- `pnpm --filter @xiaipet/api typecheck`
- `pnpm --filter @xiaipet/api test`
- `pnpm --filter @xiaipet/api db:generate`
- `rg "model User|model Order|Decimal|Json" apps/api/prisma/schema.prisma`
</verification>

<success_criteria>
- Prisma tooling exists in `apps/api`.
- `DATABASE_URL` is validated and documented with placeholders only.
- Prisma schema covers every DB-01 model family.
- Order snapshots and runtime config values have JSON storage.
</success_criteria>
