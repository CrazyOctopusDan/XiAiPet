---
phase: 8
plan: 08-02
name: Local MySQL and seed pipeline
type: execute
wave: 1
depends_on:
  - 08-01
files_modified:
  - docker-compose.dev.yml
  - apps/api/prisma/seed.ts
  - apps/api/prisma/seed.test.ts
  - apps/api/package.json
  - apps/api/.env.example
autonomous: true
requirements:
  - DB-01
must_haves:
  truths:
    - Local MySQL 8 can be started without changing production docker-compose.yml.
    - Seed data includes runtime config, merchant user, customer user, categories and products.
    - A blocking local migration step exists for Prisma schema verification.
  artifacts:
    - path: docker-compose.dev.yml
      provides: Dev-only MySQL 8 service
    - path: apps/api/prisma/seed.ts
      provides: Deterministic baseline data for local API development
    - path: apps/api/prisma/seed.test.ts
      provides: Seed fixture validation without requiring a live database
    - path: apps/api/prisma/migrations/
      provides: Prisma migration history generated from schema
  key_links:
    - from: docker-compose.dev.yml
      to: apps/api/.env.example
      via: local DATABASE_URL host, port and credentials
      pattern: "3307|xiaipet_local_password"
    - from: apps/api/prisma/seed.ts
      to: packages/shared/src/types/runtime-config.ts
      via: runtime config section IDs
      pattern: "store-profile|delivery-rules|membership-tiers|banner|custom-notice"
---

<objective>
Provide repeatable local MySQL 8 development setup and seed data for the new MySQL source of truth.
</objective>

<threat_model>
T8-02: Local database setup accidentally becomes production infrastructure or commits reusable production secrets.
Severity: high.
Mitigation: local MySQL lives in `docker-compose.dev.yml` with explicit local-only credentials; production `docker-compose.yml` continues to use managed RDS via `DATABASE_URL`.
</threat_model>

<tasks>
  <task id="08-02-01" type="standard">
    <name>Add local MySQL compose file</name>
    <files>
      <file>docker-compose.dev.yml</file>
    </files>
    <read_first>
      <file>docker-compose.yml</file>
      <file>apps/api/.env.example</file>
      <file>.planning/phases/08-mysql-data-model-and-migration-pipeline/08-CONTEXT.md</file>
    </read_first>
    <action>
      Create `docker-compose.dev.yml` with service `mysql`, image `mysql:8.0`, container name `xiaipet-mysql-dev`, `MYSQL_ROOT_PASSWORD=xiaipet_local_root`, `MYSQL_DATABASE=xiaipet_dev`, `MYSQL_USER=xiaipet`, `MYSQL_PASSWORD=xiaipet_local_password`, port mapping `3307:3306`, volume `xiaipet_mysql_dev:/var/lib/mysql`, and healthcheck `mysqladmin ping -h 127.0.0.1 -uxiaipet -pxiaipet_local_password`. Do not modify production `docker-compose.yml` to add MySQL.
    </action>
    <verify>
      <automated>docker compose -f docker-compose.dev.yml config</automated>
    </verify>
    <acceptance_criteria>
      `docker-compose.dev.yml` contains `image: mysql:8.0`, `3307:3306`, and `xiaipet_mysql_dev`; root `docker-compose.yml` still contains no `mysql:` service.
    </acceptance_criteria>
    <done>Local MySQL is available through a dev-only Compose file.</done>
  </task>

  <task id="08-02-02" type="standard">
    <name>Create deterministic seed data</name>
    <files>
      <file>apps/api/prisma/seed.ts</file>
    </files>
    <read_first>
      <file>apps/api/prisma/schema.prisma</file>
      <file>packages/shared/src/types/runtime-config.ts</file>
      <file>packages/shared/src/types/catalog-admin.ts</file>
      <file>packages/shared/src/schema/runtime-config.ts</file>
      <file>packages/shared/src/schema/catalog-admin.ts</file>
    </read_first>
    <action>
      Create `apps/api/prisma/seed.ts` using Prisma Client. Seed these deterministic records with upsert: one merchant user with openid `merchant-openid-dev`, one customer user with openid `customer-openid-dev`, one balance account for `customer-openid-dev` with balance `200.00`, categories `cat-cakes` and `cat-snacks`, products `prod-birthday-cake` and `prod-paw-cookie`, and all five runtime config sections `store-profile`, `delivery-rules`, `membership-tiers`, `banner`, `custom-notice`. Use local placeholder image references such as `cloud://dev-placeholder/banner.png` until OSS migration happens in Phase 11.
    </action>
    <verify>
      <automated>pnpm --filter @xiaipet/api db:seed</automated>
    </verify>
    <acceptance_criteria>
      `apps/api/prisma/seed.ts` contains `merchant-openid-dev`, `customer-openid-dev`, `prod-birthday-cake`, `prod-paw-cookie`, and all five runtime config section IDs.
    </acceptance_criteria>
    <done>Seed script can populate baseline local data.</done>
  </task>

  <task id="08-02-03" type="standard">
    <name>Test seed fixture builders</name>
    <files>
      <file>apps/api/prisma/seed.test.ts</file>
      <file>apps/api/prisma/seed.ts</file>
    </files>
    <read_first>
      <file>apps/api/prisma/seed.ts</file>
      <file>apps/api/vitest.config.ts</file>
    </read_first>
    <action>
      Create `apps/api/prisma/seed.test.ts` that imports seed fixture builders from `seed.ts` without connecting to MySQL. Export pure helper functions from `seed.ts` such as `buildSeedRuntimeConfigSections()` and `buildSeedCatalog()` so tests can assert section IDs, product IDs, positive stock, and non-negative decimal strings before actual seeding runs.
    </action>
    <verify>
      <automated>pnpm --filter @xiaipet/api test -- prisma/seed.test.ts</automated>
    </verify>
    <acceptance_criteria>
      `apps/api/prisma/seed.test.ts` asserts `RUNTIME_CONFIG_SECTION_IDS` length is 5 and seeded products include `prod-birthday-cake`.
    </acceptance_criteria>
    <done>Seed fixture builders are covered without requiring a live database connection.</done>
  </task>

  <task id="08-02-04" type="standard">
    <name>Run blocking local migration verification</name>
    <files>
      <file>apps/api/prisma/migrations/</file>
    </files>
    <read_first>
      <file>apps/api/prisma/schema.prisma</file>
      <file>docker-compose.dev.yml</file>
      <file>apps/api/package.json</file>
    </read_first>
    <action>
      Run the blocking schema verification once local MySQL is available: `docker compose -f docker-compose.dev.yml up -d mysql`, then `pnpm --filter @xiaipet/api db:migrate:dev -- --name init_mysql_data_layer`, then `pnpm --filter @xiaipet/api db:generate`, then `pnpm --filter @xiaipet/api db:seed`. If Docker is unavailable in the execution environment, record the blocker in the plan summary and keep static checks green.
    </action>
    <verify>
      <automated>pnpm --filter @xiaipet/api db:migrate:dev -- --name init_mysql_data_layer</automated>
    </verify>
    <acceptance_criteria>
      After running with Docker/MySQL available, `apps/api/prisma/migrations/` exists and `pnpm --filter @xiaipet/api db:generate` exits 0.
    </acceptance_criteria>
    <done>Schema migration and Prisma client generation have been attempted against local MySQL or explicitly recorded as Docker-blocked.</done>
  </task>
</tasks>

<verification>
Run:
- `docker compose -f docker-compose.dev.yml config`
- `docker compose -f docker-compose.dev.yml up -d mysql`
- `pnpm --filter @xiaipet/api db:migrate:dev -- --name init_mysql_data_layer`
- `pnpm --filter @xiaipet/api db:generate`
- `pnpm --filter @xiaipet/api db:seed`
- `pnpm --filter @xiaipet/api test -- prisma/seed.test.ts`
</verification>

<success_criteria>
- Local MySQL 8 can be started without touching RDS.
- Prisma migrations can be generated and applied locally.
- Seed data creates enough baseline records for later API parity work.
- Production Compose does not self-host MySQL.
</success_criteria>
