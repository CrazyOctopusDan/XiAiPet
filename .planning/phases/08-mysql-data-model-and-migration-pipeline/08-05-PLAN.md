---
phase: 8
plan: 08-05
name: RDS documentation and final database verification
type: execute
wave: 4
depends_on:
  - 08-01
  - 08-02
  - 08-03
  - 08-04
files_modified:
  - docs/release/alibaba-rds.md
  - docs/release/alibaba-ecs-api.md
  - apps/api/.env.example
  - apps/api/package.json
requirements:
  - DB-01
  - DB-02
  - DB-03
  - DB-04
autonomous: true
must_haves:
  truths:
    - RDS docs include connection string, migration commands and backup expectations.
    - Docs explicitly forbid `prisma migrate reset` against RDS.
    - Final verification records DB-backed and non-DB command status.
  artifacts:
    - path: docs/release/alibaba-rds.md
      provides: RDS setup, migration, backup and verification runbook
    - path: docs/release/alibaba-ecs-api.md
      provides: API deployment runbook updated with DATABASE_URL/RDS linkage
    - path: apps/api/package.json
      provides: Database command entrypoints for operator docs
  key_links:
    - from: docs/release/alibaba-ecs-api.md
      to: docs/release/alibaba-rds.md
      via: DATABASE_URL setup reference
      pattern: "DATABASE_URL|alibaba-rds.md"
    - from: docs/release/alibaba-rds.md
      to: apps/api/package.json
      via: documented Prisma commands
      pattern: "db:migrate:deploy|db:verify"
---

<objective>
Document RDS setup, local-to-RDS migration commands, backup expectations and the final Phase 8 verification checklist for a frontend developer.
</objective>

<threat_model>
T8-05: RDS deployment docs cause accidental production reset or leak database credentials.
Severity: high.
Mitigation: docs separate dev reset commands from production deploy commands, forbid `migrate reset` on RDS, and use placeholder-only connection strings.
</threat_model>

<tasks>
  <task id="08-05-01" type="standard">
    <name>Create Alibaba RDS runbook</name>
    <files>
      <file>docs/release/alibaba-rds.md</file>
    </files>
    <read_first>
      <file>docs/release/alibaba-ecs-api.md</file>
      <file>apps/api/.env.example</file>
      <file>.planning/phases/08-mysql-data-model-and-migration-pipeline/08-RESEARCH.md</file>
    </read_first>
    <action>
      Create `docs/release/alibaba-rds.md` with sections `## What This Covers`, `## Local MySQL`, `## RDS Connection String`, `## Development Commands`, `## Production Migration Commands`, `## Backup Expectations`, `## Verification`, and `## Troubleshooting`. Include placeholder connection string `mysql://<user>:<password>@<rds-host>:3306/<database>?sslaccept=strict` and explicitly state: `Never run prisma migrate reset against RDS`.
    </action>
    <verify>
      <automated>rg "Never run prisma migrate reset against RDS|mysql://<user>:<password>@<rds-host>:3306/<database>|Production Migration Commands|Backup Expectations" docs/release/alibaba-rds.md</automated>
    </verify>
    <acceptance_criteria>
      RDS docs contain the exact warning `Never run prisma migrate reset against RDS` and do not contain a real password, IP address or hostname.
    </acceptance_criteria>
    <done>RDS runbook exists with safe placeholder-only connection guidance.</done>
  </task>

  <task id="08-05-02" type="standard">
    <name>Link RDS docs from ECS runbook</name>
    <files>
      <file>docs/release/alibaba-ecs-api.md</file>
    </files>
    <read_first>
      <file>docs/release/alibaba-ecs-api.md</file>
      <file>docs/release/alibaba-rds.md</file>
      <file>docker-compose.yml</file>
    </read_first>
    <action>
      Update `docs/release/alibaba-ecs-api.md` to reference `docs/release/alibaba-rds.md` in the environment configuration section. Add `DATABASE_URL` to the production `.env.production` example, using placeholder text only. State that ECS runs API/Nginx only while Alibaba Cloud RDS hosts MySQL.
    </action>
    <verify>
      <automated>rg "DATABASE_URL|alibaba-rds.md|RDS hosts MySQL" docs/release/alibaba-ecs-api.md</automated>
    </verify>
    <acceptance_criteria>
      ECS docs mention `DATABASE_URL`, link or name `docs/release/alibaba-rds.md`, and state that RDS hosts MySQL.
    </acceptance_criteria>
    <done>ECS API runbook points to RDS setup and includes `DATABASE_URL` placeholder guidance.</done>
  </task>

  <task id="08-05-03" type="standard">
    <name>Run final Phase 8 verification</name>
    <files>
      <file>apps/api/package.json</file>
      <file>apps/api/prisma/schema.prisma</file>
      <file>apps/api/scripts/verify-data.ts</file>
    </files>
    <read_first>
      <file>apps/api/package.json</file>
      <file>apps/api/prisma/schema.prisma</file>
      <file>apps/api/scripts/verify-data.ts</file>
    </read_first>
    <action>
      Run final local verification in this order: `pnpm --filter @xiaipet/api db:generate`, `pnpm --filter @xiaipet/api db:migrate:dev -- --name verify_phase_8_schema` only if no migration exists yet, `pnpm --filter @xiaipet/api db:seed`, `pnpm --filter @xiaipet/api db:verify`, `pnpm --filter @xiaipet/api typecheck`, `pnpm --filter @xiaipet/api test`, and `pnpm --filter @xiaipet/api build`. If Docker/MySQL is unavailable, record that only DB-backed commands were not run and include static/code test results in the summary.
    </action>
    <verify>
      <automated>pnpm --filter @xiaipet/api typecheck && pnpm --filter @xiaipet/api test && pnpm --filter @xiaipet/api build</automated>
    </verify>
    <acceptance_criteria>
      Final summary records pass/fail status for `db:generate`, `db:migrate:dev`, `db:seed`, `db:verify`, `typecheck`, `test`, and `build`.
    </acceptance_criteria>
    <done>Phase 8 final summary includes the status of every required verification command.</done>
  </task>

  <task id="08-05-04" type="standard">
    <name>Prepare GSD completion checks</name>
    <files>
      <file>.planning/REQUIREMENTS.md</file>
      <file>.planning/ROADMAP.md</file>
      <file>.planning/phases/08-mysql-data-model-and-migration-pipeline/08-VALIDATION.md</file>
    </files>
    <read_first>
      <file>.planning/REQUIREMENTS.md</file>
      <file>.planning/ROADMAP.md</file>
      <file>.planning/phases/08-mysql-data-model-and-migration-pipeline/08-VALIDATION.md</file>
    </read_first>
    <action>
      After implementation and verification, update GSD execution summaries only through the execute-phase summary process. Do not mark DB-01 through DB-04 complete until `08-01-SUMMARY.md` through `08-05-SUMMARY.md` exist and the final summary states whether DB-backed commands ran.
    </action>
    <verify>
      <automated>test -f .planning/phases/08-mysql-data-model-and-migration-pipeline/08-05-SUMMARY.md</automated>
    </verify>
    <acceptance_criteria>
      Phase 8 completion requires summaries for all five plans and explicit DB verification status.
    </acceptance_criteria>
    <done>Completion criteria prevent marking Phase 8 done before summaries and DB verification status exist.</done>
  </task>
</tasks>

<verification>
Run:
- `rg "Never run prisma migrate reset against RDS" docs/release/alibaba-rds.md`
- `rg "DATABASE_URL|RDS hosts MySQL" docs/release/alibaba-ecs-api.md`
- `pnpm --filter @xiaipet/api typecheck`
- `pnpm --filter @xiaipet/api test`
- `pnpm --filter @xiaipet/api build`
- DB-backed commands from task 08-05-03 where Docker/MySQL is available
</verification>

<success_criteria>
- RDS setup docs explain connection string, migration commands and backup expectations.
- Docs make production-dangerous reset commands explicit.
- Phase 8 has a final verification path for both code and database state.
- The user can follow local MySQL commands before touching RDS.
</success_criteria>
