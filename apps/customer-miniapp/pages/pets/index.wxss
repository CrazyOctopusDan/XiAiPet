.pets-page {
  min-height: 100vh;
  padding-bottom: calc(180rpx + env(safe-area-inset-bottom));
  background:
    radial-gradient(circle at 14% 0%, rgba(246, 227, 150, 0.22) 0, transparent 30%),
    linear-gradient(180deg, #FFFDF5 0%, #FFF9DF 54%, #F6E396 100%);
  color: #40535C;
  box-sizing: border-box;
}

.pet-card-head {
  display: flex;
  align-items: center;
}

.pets-shell {
  padding: 26rpx 24rpx 0;
}

.pet-name,
.pet-meta,
.pet-allergy-label,
.pet-allergy-copy,
.empty-title,
.empty-copy {
  display: block;
}

.pet-list {
  margin-top: 8rpx;
}

.pet-card {
  margin-top: 18rpx;
  padding: 30rpx 26rpx;
  border: 2rpx solid rgba(80, 104, 105, 0.06);
  border-radius: 32rpx;
  background: rgba(255, 255, 255, 0.88);
  box-shadow: 0 16rpx 34rpx rgba(80, 104, 105, 0.08);
  box-sizing: border-box;
}

.pet-card-head {
  align-items: flex-start;
  justify-content: space-between;
  gap: 20rpx;
}

.pet-name {
  font-size: 32rpx;
  line-height: 1.25;
  font-weight: 900;
  color: #40535C;
}

.pet-meta {
  margin-top: 10rpx;
  font-size: 23rpx;
  line-height: 1.3;
  font-weight: 700;
  color: #7B8B92;
}

.pet-edit {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  min-width: 132rpx;
  height: 64rpx;
  border-radius: 999rpx;
  background: #F6E396;
  font-size: 23rpx;
  font-weight: 900;
  color: #40535C;
}

.pet-allergy-label {
  margin-top: 22rpx;
  font-size: 22rpx;
  line-height: 1.2;
  font-weight: 800;
  color: #7B8B92;
}

.pet-allergy-copy {
  margin-top: 10rpx;
  font-size: 25rpx;
  line-height: 1.6;
  font-weight: 700;
  color: #7B8B92;
}

.empty-state {
  margin-top: 8rpx;
  padding: 80rpx 32rpx;
  border: 2rpx dashed rgba(80, 104, 105, 0.14);
  border-radius: 32rpx;
  background: rgba(255, 255, 255, 0.72);
  text-align: center;
  box-sizing: border-box;
}

.empty-title {
  font-size: 34rpx;
  line-height: 1.25;
  font-weight: 900;
  color: #40535C;
}

.empty-copy {
  margin-top: 16rpx;
  font-size: 24rpx;
  line-height: 1.6;
  font-weight: 700;
  color: #7B8B92;
}

.pet-fixed-action {
  position: fixed;
  right: 0;
  bottom: 0;
  left: 0;
  z-index: 20;
  padding: 18rpx 24rpx calc(18rpx + env(safe-area-inset-bottom));
  background: rgba(255, 253, 246, 0.94);
  backdrop-filter: blur(18rpx);
  box-shadow: 0 -12rpx 32rpx rgba(82, 116, 126, 0.08);
  box-sizing: border-box;
}

.pet-add-button {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  height: 96rpx;
  padding: 0;
  border-radius: 999rpx;
  background: #F6E396;
  font-size: 30rpx;
  line-height: 96rpx;
  font-weight: 900;
  color: #40535C;
  box-shadow: 0 18rpx 38rpx rgba(82, 116, 126, 0.2);
}

.pet-add-button::after {
  border: 0;
}
