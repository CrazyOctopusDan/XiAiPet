.address-page {
  min-height: 100vh;
  padding-bottom: calc(180rpx + env(safe-area-inset-bottom));
  background:
    radial-gradient(circle at 14% 0%, rgba(246, 227, 150, 0.22) 0, transparent 30%),
    linear-gradient(180deg, #FFFDF5 0%, #FFF9DF 54%, #F6E396 100%);
  color: #40535C;
  box-sizing: border-box;
}

.address-card-head,
.address-actions {
  display: flex;
  align-items: center;
}

.address-shell {
  padding: 26rpx 24rpx 0;
}

.type-pill-label,
.type-pill-hint,
.address-name,
.address-phone,
.address-detail,
.empty-title,
.empty-copy {
  display: block;
}

.type-switch-card {
  display: flex;
  gap: 18rpx;
  margin-top: 8rpx;
}

.type-pill {
  flex: 1;
  min-width: 0;
  padding: 24rpx 22rpx;
  border: 2rpx solid rgba(80, 104, 105, 0.08);
  border-radius: 30rpx;
  background: rgba(255, 255, 255, 0.86);
  box-shadow: 0 14rpx 30rpx rgba(80, 104, 105, 0.07);
  box-sizing: border-box;
}

.type-pill.active {
  border-color: rgba(255, 230, 163, 0.28);
  background: linear-gradient(145deg, #FFF8D8 0%, #F6E396 62%, #F2C46F 100%);
  box-shadow: 0 18rpx 38rpx rgba(82, 116, 126, 0.18);
}

.type-pill-label {
  font-size: 28rpx;
  line-height: 1.2;
  font-weight: 900;
  color: #40535C;
}

.type-pill.active .type-pill-label,
.type-pill.active .type-pill-hint {
  color: #40535C;
}

.type-pill-hint {
  margin-top: 10rpx;
  font-size: 22rpx;
  line-height: 1.45;
  font-weight: 700;
  color: #7B8B92;
}

.address-list {
  margin-top: 22rpx;
}

.address-card {
  margin-top: 18rpx;
  padding: 30rpx 26rpx;
  border: 2rpx solid rgba(80, 104, 105, 0.06);
  border-radius: 32rpx;
  background: rgba(255, 255, 255, 0.88);
  box-shadow: 0 16rpx 34rpx rgba(80, 104, 105, 0.08);
  box-sizing: border-box;
}

.address-card-head {
  align-items: flex-start;
  justify-content: space-between;
  gap: 20rpx;
}

.address-name {
  font-size: 32rpx;
  line-height: 1.25;
  font-weight: 900;
  color: #40535C;
}

.address-phone {
  margin-top: 8rpx;
  font-size: 23rpx;
  font-weight: 700;
  color: #7B8B92;
}

.address-card-meta {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 10rpx;
}

.address-tag,
.address-current {
  padding: 8rpx 16rpx;
  border-radius: 999rpx;
  font-size: 20rpx;
  line-height: 1.2;
  font-weight: 800;
}

.address-tag {
  background: #F6E396;
  color: #6B7C84;
}

.address-current {
  background: rgba(64, 83, 92, 0.1);
  color: #7B8B92;
}

.address-detail {
  margin-top: 18rpx;
  font-size: 25rpx;
  line-height: 1.6;
  font-weight: 700;
  color: #7B8B92;
}

.address-actions {
  gap: 16rpx;
  margin-top: 24rpx;
}

.address-action {
  display: flex;
  align-items: center;
  justify-content: center;
  min-width: 180rpx;
  height: 72rpx;
  border-radius: 999rpx;
  font-size: 24rpx;
  font-weight: 900;
  box-sizing: border-box;
}

.address-action.ghost {
  border: 2rpx solid rgba(64, 83, 92, 0.16);
  color: #7B8B92;
}

.address-action.primary {
  background: #F6E396;
  color: #40535C;
}

.empty-state {
  margin-top: 22rpx;
  padding: 80rpx 32rpx;
  border: 2rpx dashed rgba(80, 104, 105, 0.14);
  border-radius: 32rpx;
  background: rgba(255, 255, 255, 0.72);
  text-align: center;
  box-sizing: border-box;
}

.empty-title {
  font-size: 34rpx;
  line-height: 1.25;
  font-weight: 900;
  color: #40535C;
}

.empty-copy {
  margin-top: 16rpx;
  font-size: 24rpx;
  line-height: 1.6;
  font-weight: 700;
  color: #7B8B92;
}

.address-fixed-action {
  position: fixed;
  right: 0;
  bottom: 0;
  left: 0;
  z-index: 20;
  padding: 18rpx 24rpx calc(18rpx + env(safe-area-inset-bottom));
  background: rgba(255, 253, 246, 0.94);
  backdrop-filter: blur(18rpx);
  box-shadow: 0 -12rpx 32rpx rgba(82, 116, 126, 0.08);
  box-sizing: border-box;
}

.address-add-button {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  height: 96rpx;
  padding: 0;
  border-radius: 999rpx;
  background: #F6E396;
  font-size: 30rpx;
  line-height: 96rpx;
  font-weight: 900;
  color: #40535C;
  box-shadow: 0 18rpx 38rpx rgba(82, 116, 126, 0.2);
}

.address-add-button::after {
  border: 0;
}
